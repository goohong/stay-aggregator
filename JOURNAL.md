# 진행 기록

의사결정과 AI 활용을 함께 적는다. 처음엔 AI 활용만 `docs/ai-log.md`에 따로
적었는데, 이 프로젝트는 혼자 개발이고 거의 모든 코드를 AI 와 함께 써서
"무슨 일이 있었나"와 "AI 와 무슨 일이 있었나"가 같은 이야기였다. 합쳤다.

각 결정은 이 표기로 남긴다.

```
AI 안 → 반박 → 검증 → 결과
```

## 주고받은 반박 모음

사용자와 AI 가 서로의 주장을 반박하거나 정정한 곳을 시간순으로 모았다. 대화 기록 원본에서 찾아 정리했다.
자세한 경위가 ADR 이나 아래 날짜별 기록에 있으면 마지막 칸에 적었다.

- **짚은 쪽**: 사용자 = 사용자가 AI 주장을 반박 / AI = AI 가 사용자 생각을 반박 / 검토 AI = 맥락을 따로 받은 다른 AI 세션 / 스스로 = AI 가 검증하다 자기 주장을 뒤집음

### 작업 방식 (9/13)

| 짚은 쪽 | 처음 주장 | 반박 | 결과 | 자세히 |
|---|---|---|---|---|
| 사용자 | AI 가 직무 설명 원문 없이 계획을 세움 | 직무 설명이 중요한데 없이 어떻게 계획했나 | 과거 기록에서 원문을 찾아 반영 | — |
| 사용자 | AI 가 승인 없이 코드를 쓰기 시작함 | 같이 승인하면서 가기로 한 것 아닌가 | 저장소를 커밋 0개로 되돌림 | Day 0 |
| 사용자 | AI 가 "여러 개를 묶어 한 번에 승인"하는 방식과 자율 단계를 제안 | 자율로 하길 원하지 않는다. 하나하나 같이 따지자 | 갈림길마다 선택지 제시 → 사용자 결정 | ADR-0001 |
| 사용자 | AI 가 가용 시간을 정하고, 사용자가 직접 타이핑하는 절차를 제안 | 시간을 자원으로 두지 말라. 직접 타이핑의 목적이 대충 볼까 봐서라면 걱정 말라 | 시간 제약과 직접 타이핑 절차를 뺌 | — |
| 사용자 | AI 가 이미 쓴 코드를 유지하고 소급 검토하자고 권함 | 이미 내린 결정에 편향되지 않을 자신이 없으면 되돌리라 | 되돌리고 처음부터 같이 정함 | Day 0 |
| 사용자 | AI 가 "원장", "소유", "관통", "끝에서 끝" 같은 번역어를 씀 | 억지 번역이라 이해가 안 된다. 원래 영어 기술 용어를 쓰라 | 기술 용어는 영어 그대로 쓰기로 함 | — |
| 사용자 | AI 가 "설명 못 하면 더 단순한 코드로 다시 쓴다"를 규칙으로 제안 | 설명 가능성이 구현 기준이 되면 안 된다. 모르면 공부하고, 모르면 승인하지 않겠다 | 규칙에서 뺌 | ADR-0002 |
| 사용자 | AI 가 할 일 목록에서 API 문서 자동화 등 코드 밖 항목을 빠뜨림 | 언급했는데 목록에 없었나 | 목록에 추가 | — |
| 사용자 | AI 가 사용자가 미리 쳐 둔 메시지를 결정 근거로 오해 | 결정 근거가 아니라 일하는 방식을 말한 것. 선택 항목은 전부 챙긴다 | 선택 항목 전부를 범위로 둠 | — |
| 사용자 | AI 가 구현 언어로 Java 를 권함 (AI 가 만든 Kotlin 코드를 설명하기 어렵다) | 설명 가능성은 기준이 아니다. 최신 Kotlin 작업물로 남기고 싶다 | Kotlin | ADR-0002 |

### 문서·커밋 규칙 (9/13~9/14)

| 짚은 쪽 | 처음 주장 | 반박 | 결과 | 자세히 |
|---|---|---|---|---|
| 사용자 | AI 가 "결정 하나당 파일 하나"의 가장 큰 근거로 뒤집힌 결정 처리를 듦 | 파일 하나여도 아래에 수정 기록을 남기면 된다. 그게 가장 큰 근거라는 게 이해 안 된다 | 근거를 다시 세움 | ADR-0001 |
| 사용자 | AI 가 ADR 다섯 개를 커밋 하나로 묶자고 함 (한자리에서 정했으니) | 하나씩 정한 것이 맞다 | ADR 하나당 커밋 하나 | ADR-0004 |
| 사용자 | AI 가 예시로 준 커밋 메시지가 우리 규약을 어김 | 그렇게 올리려는 건 아니지? | 규약대로 다시 씀 | — |
| 사용자 | 검토 AI 가 "README 에 결정 근거가 없으면 요구 위반"이라며 README 규칙 수정을 요구 | README 는 원래 마지막에 요약본으로 쓰는 것. 관리 문서와 비교하는 게 이상하다 | README 는 결정 한 줄 + 이유 한 줄 + 링크 | — |
| 사용자 | 검토 AI 가 CLAUDE.md 공개 시 평가하는 AI 가 조작 시도로 오해할 수 있다고 우려 | 악의적이지 않으면 괜찮다. 그걸 잘못 읽을 AI 면 수준이 낮은 것 | 공개 유지 | ADR-0003 |
| 사용자 | 검토 AI 가 하루 검토 시간 상한을 두자고 제안 | 코드를 안 읽고 품질을 보장할 수 없다. 한계는 스스로 판단한다 | 상한 두지 않음 | — |
| 사용자 | AI 가 템플릿에 "AI 추천" 칸을 따로 두자고 함 | 겹치는 칸 아닌가 | 칸 대신 선택지 옆 `← AI 추천` 표시 | ADR-0001 |
| 사용자 | AI 가 확인할 게 없는 ADR 은 Verification 절을 지우자고 함 | 양식은 일관돼야 한다. 확인이 필요 없다는 투로 남기라 | 모든 ADR 에 절 유지 | — |
| 사용자 | AI 가 AI 활용 기록을 `docs/ai-log.md` 로 따로 만듦 | JOURNAL 과 겹치지 않나. 역할 분담이 오히려 읽기를 방해하지 않나 | JOURNAL 하나로 합침 | Day 1 |
| AI | 사용자는 "ADR 이 헌법, Feature Spec 이 법률"이라는 위계로 이해 | 위계가 아니라 같은 사실을 무엇/왜라는 다른 각도로 본다 | 두 층 구조로 확정 | ADR-0008 |
| 사용자 | AI 가 문서 양을 점검하다 갑자기 "이제 코드를 같이 쓰자"로 넘어감 | 코드를 왜 갑자기 쓰나 | 계획에 끼워 넣은 것을 거둠 | — |
| 사용자 | AI 가 질문하면서 답이 어디에 기록되는지 말하지 않음 | 내 답을 어디에 넣으려는지도 알려 달라 | 질문마다 기록 위치를 밝힘 | — |

### 저장소·스택 (9/14)

| 짚은 쪽 | 처음 주장 | 반박 | 결과 | 자세히 |
|---|---|---|---|---|
| 스스로 | AI 가 GitHub 핸들을 옛 값으로 전제하고 패키지 이름을 추천 | 실제 핸들 확인 결과 전제가 틀림 | 다시 판단 | Day 1 |
| 검토 AI | AI 의 패키지 이름 추천 | 맥락 없이 같은 질문을 받은 다른 AI 가 다른 답을 냄 | `com.stayaggregator` | Day 1 |
| AI | 사용자는 멀티 모듈이 너무 크다고 봄 (예전 프로젝트에서 빌드 설정이 꼬인 경험) | Mock 을 테스트 코드 안에 두면 앱 설정·DB 파일을 같이 물려받아 충돌 | Mock 은 별도 모듈 | ADR-0005 |
| AI | 사용자는 테스트용 모킹 라이브러리면 충분하다고 봄 | 요구사항은 연동 동작을 실제로 띄워 확인하므로 별도 포트의 Mock 서버가 필요 | 독립 Mock 서버 | ADR-0005 |
| 사용자 | AI 가 날짜 처리 문제와 네 번째 장애 시나리오를 모듈 결정에 섞어 말함 | 모듈과 무관한데 그냥 얘기한 건가 | 별개 문제로 분리 | — |
| 사용자 | AI 가 Gradle·Spring Boot 버전을 혼자 정함 | 같이 논의했어야 한다. 근거와 함께 남기자 | 버전 결정 ADR 작성 | ADR-0007 |
| 사용자 | AI 의 근거가 "안정적인 것 중 최신", "JDK 21 이 로컬에 있다" | 그게 근거인가. 로컬에 있다는 말은 말이 안 된다. 제대로 조사했나 | 공식 문서·JEP·호환표로 다시 조사 | ADR-0007 |
| 스스로 | AI 가 "JDK 가 없으면 빌드 실패" → 다시 "Gradle 이 자동으로 받는다" | 둘 다 틀림. 캐시에 있던 JDK 였고, 자동 다운로드는 플러그인이 있어야 함 | 근거 교체 | ADR-0007 |
| 사용자 | AI 가 Spring Boot 4 의 약점으로 "자료가 적다"를 듦 | 나는 실사례보다 공식 문서로 판단한다 | 약점의 무게가 줄어 Boot 4 채택 | ADR-0007 |
| 스스로 | 검토 AI 가 Gradle 9.7.1 이 실행 환경 위험이 더 작다고 함 | 공식 호환표상 9.4.1 과 실행 JVM 범위가 같음 | 그 근거는 기각 | ADR-0007 |
| 사용자 | AI 가 "이미 검증했으니 9.4.1 유지" | 9.4.1 이 9.7.1 보다 나은 근거도 없지 않나 | 9.7.1 | ADR-0007 |
| AI | 사용자는 네 버전이 모두 LTS 라고 봄 | LTS 는 JDK 에만 있는 개념. 나머지는 라인별 지원 기간 | ADR 에 LTS 로 잘못 쓰지 않음 | ADR-0007 |
| 사용자 | AI 의 Kotlin 버전 근거가 "최신이고 문제없다" | 그 검증을 해 달라는 것 | 우리가 고른 Gradle 을 지원 범위에 담는 버전이라는 근거로 교체 | ADR-0007 |

### 요구사항·기록 (9/14)

| 짚은 쪽 | 처음 주장 | 반박 | 결과 | 자세히 |
|---|---|---|---|---|
| 사용자 | AI 가 요구사항 문서의 필요한 절만 골라 읽고 설계를 논의 | 가장 중요한 문서다. 최소 세 번 읽고 대화하라 | 전체 3회독 후 오픈 질문 13개로 확장 | Day 2 밤 |
| 사용자 | AI 가 조사 도중 사이트 접속이 차단됐다며 멈춤 | 그럼 브라우저로 직접 보라 | 브라우저로 화면을 직접 확인 | Day 2 밤 |
| 사용자 | AI 가 커밋 시각을 실제 결정 시각으로 옮기는 안을 선택지로 꺼냄 | 원래 적힌 시각을 그대로 유지하라는 뜻이었다 | 원래 시각 유지 | Day 2 밤 |
| 사용자 | AI 가 요구사항 문서와 몇 글자 겹친다고 자연스러운 문장을 고침 | 이 작업의 성격이 드러나지만 않으면 괜찮다. 목적에서 벗어난 행위를 위한 행위 | 겹침 검사를 커밋 조건에서 뺌 | Day 2 밤 |
| 사용자 | AI 가 방향 의견을 요청받자 남은 시간을 근거로 "정하자마자 구현", "선택 항목은 설계만" 제안 | 합의한 방식을 왜 마음대로 깨나 | 거둬들임 | Day 2 밤 |

### 도메인 설계 (9/14)

| 짚은 쪽 | 처음 주장 | 반박 | 결과 | 자세히 |
|---|---|---|---|---|
| 사용자 | AI 가 A·B 두 공급사만 보고 요금 필드를 짬 | 공급사가 늘어날 걸 전제하기로 한 원칙은 어디 반영됐나 | 공급사별 변환을 어댑터로 분리 | Day 2 |
| AI | 사용자는 예전 프로젝트의 컴포짓 패턴을 떠올림 | 그건 여럿 중 하나를 골라 쓰는 폴백이고, 여긴 전부 불러 합치는 구조 | 어댑터 + 병렬 호출 | Day 2 |
| 사용자 | AI 가 "공급사 2~3개면 코루틴과 Reactor 성능 차이가 없다" | 공급사가 많아질 환경을 전제해야 하지 않나 | 동시 호출 수 제한이 근거가 되어 Reactor | ADR-0009 |
| AI | 사용자가 Semaphore 를 직접 쓰는 이점을 물음 | 공급사별 세밀한 제어는 얻지만 지금 확인된 요구가 아니다 | Reactor 연산자로 제한 | ADR-0009 |
| 사용자 | AI 가 금액 값 객체를 "오버엔지니어링 위험"으로 표시 | 값 객체는 오히려 좋은 패턴이다. 잃는 게 클래스 하나뿐 아닌가 | 값 객체 채택, 오버엔지니어링 표현 철회 | — |
| AI | 사용자가 음수 금액을 막자고 함 | 할인·환불은 음수로 표현하고 싶어질 수 있다 | 금액은 0 이상, 음수 개념은 별도 개념이나 메서드로 | — |
| 사용자 | AI 가 통화 코드 검증 위치를 정하지 않고 넘어감 | 금액 객체가 문자열을 받아 직접 검증하는 게 낫지 않나 | 금액 객체 안에서 검증 | — |
| 사용자 | AI 가 "세금 포함 총액이 필수"를 정해진 것처럼 말함 | 나는 답한 적 없다. 요구사항에 맞는가, 응답값이 총액인가 | 요구사항은 우리 판단에 맡겼음을 확인하고 다시 따짐 | — |
| 사용자 | AI 가 1박 평균은 "나누기라서 지어낸 값"이라고만 설명 | 평일·주말 가격이 다를 수 있다. 전제부터 따져 보자 | 예시 데이터에서도 날짜별 요금이 다름을 확인. 조회 단위도 기간 전체 | — |
| 스스로 | AI 가 총액 하나를 기준 필드로 올리자고 함 | 조건이 다른 요금을 싸다고 비교하게 만들 수 있다 | 요금과 조건을 한 덩어리로 | — |
| 사용자 | AI 가 알 수 없는 조건 표시 필드까지 제안 | 조식까지만 비교 대상으로 두고 나머지는 어댑터가 통제 | 조건은 조식만 표준으로 | — |
| 사용자 | AI 가 공급사 원본을 표준 모델에 넣자고 함 | 어댑터를 거친 뒤 원본을 쓸 일이 없다 | 표준 모델에서 원본 제외 | — |
| 사용자 | AI 가 원본을 로그로 남기자고 함 | 요구사항 중 전체 원본 로그가 필요한 곳이 있나. 조식만 특별 대우할 가치가 있나 | 요구사항·업계 조사 후 판단 | research/rate-display |
| 사용자 | AI 가 "보존"을 원본 저장으로 해석해 여러 차례 논의 | 공급사 DTO 가 모든 필드를 받는다는 뜻이었다 | 오픈 질문 Q6 | Day 2 밤 |
| 사용자 | 요금 모델 논의가 요금을 저장하는 것처럼 들림 | 요금·재고는 저장 안 하는데 식별자 말고 저장할 게 있나 | 저장은 매핑뿐, 요금은 검색마다 메모리에서만 | Day 2 밤 |
| AI | 사용자는 캐시를 필수로 봄 | 요구사항 분류상 선택 항목이다 | 논의 전 | — |
| 사용자 | AI 가 "공급사 코드와 한 테이블이면 병합할 자리가 없다" | 행 두 개를 들고 있으면 되지 않나 | AI 주장이 틀렸음을 인정 | ADR-0010 |
| AI | 사용자가 자연 키 기본 키의 장점이 쓰기 속도인지 물음 | 쓰기 속도는 자연 키의 장점이 아니다. 오히려 불리한 경우가 많다 | 설명으로 정리 | — |
| 사용자 | AI 가 결정 질문에서 테이블·키·인덱스 설계로 내려감 | ERD 가 아니라 결정을 먼저 하려던 것 | 결정 수준으로 질문을 다시 세움 | ADR-0010 |
| AI | 사용자는 병합 시 식별자를 하나로 통일하는 방식을 떠올림 | 식별자를 받는 쪽이 그걸로 예약한다 | 사용자가 "식별자가 같으면 안 되겠다"고 판단해 식별자 유지 | ADR-0010 |
| 사용자 | AI 가 내부 식별자를 공급사 코드로 계산하는 방식을 권하며 트레이드오프를 "코드가 바뀌면 식별자도 바뀐다" 하나로 설명 | 트레이드오프가 그뿐인가 | 공급사 코드 역추적, 입력 표기 고정 등 큰 것 두 개를 더 찾음 | ADR-0011 |
| 사용자 | AI 가 계산 방식의 이점으로 "데이터를 잃어도 같은 값"을 듦 | 비밀 키 계산은 성능 부담이 없나. DB 를 엎는 상황까지 유지해야 하나, 마이그레이션이 있지 않나. 같은 방을 여러 개 넣을 수는 없나 | 측정해 보니 성능 부담 없음. 마이그레이션은 저장값을 옮기므로 이점이 드문 사고 대비로 좁아짐 | ADR-0011 |
| 사용자 | AI 가 "이미 매핑된 숙소에 새 객실 타입이 목록 갱신 전 조회에 먼저 나올 수 있다"는 시나리오를 흐름 설명 없이 제시 | 그게 가능한가. 우리가 저장하는 것과 조회에 넣는 값, 받아 쓰는 값이 뭔가 | 조회는 숙소 코드로만 하고 객실 타입은 응답으로 받는 흐름을 요구사항 기준으로 다시 설명 | Day 3 |
| 사용자 | AI 가 설명 중 아직 정하지 않은 저장 구조(키 표기, 컬럼 구성, 중복 방지)를 정해진 것처럼 말함 | 안 정한 걸 정한 것처럼 머릿속 구조를 꺼내지 말라. 헷갈려서 계속 지체된다 | 설명마다 요구사항·결정됨·미정을 구분 | Day 3 |
| 사용자 | AI 가 정적 정보를 목록 기준으로 하되 최대 수용 인원만 조회 값을 쓰는 예외를 권함 | 매핑이 핵심인데 예외를 두는 느낌이다. 최대 인원은 드물게 바뀐다. 바뀐 정보는 공급사가 요청하게 나중에 구현하면 된다 | 예외 없이 목록 기준. 모순은 알려진 한계, 공급사 알림은 범위 밖 확장으로 기록 | ADR-0012 |
| 사용자 | AI 가 가격·재고를 검색마다 새로 받는다는 흐름을 전제로 설명 | 그럼 이름·최대 인원은 왜 미리 저장했나. 목록을 받고 원하는 숙소로 다시 조회하는 것 아닌가 | 두 흐름(목록 동기화, 검색마다 재고·요금 조회)을 요구사항 기준으로 다시 정리. AI 가 "고객이 골라서"로 잘못 알아들은 것도 바로잡음 | Day 3 |
| 검토 AI | AI 가 어긋남 처리안을 "기록만 한다"로 설명 | 결론은 같지만 이름이 틀렸다. 본질은 고객 검색이 목록 갱신을 일으켜도 되느냐다 | 감지와 교정을 분리하는 결정으로 다시 세움 | ADR-0014 |
| 사용자 | AI 가 재고·요금 조회 응답에 매핑에 없는 객실 타입이 섞일 수 있다고 설명 | 조회는 객실 타입 코드로 하는 것 아닌가 | 조회 요청에는 숙소 코드만 들어간다는 요구사항의 요청 파라미터를 보여 주며 API 전체를 다시 정리 | Day 3 |

### 서버 실행 방식 (9/15)

| 짚은 쪽 | 처음 주장 | 반박 | 결과 | 자세히 |
|---|---|---|---|---|
| 검토 AI | AI 가 (가) `block` + 플랫폼 스레드의 근거로 "동시 요청 요구가 없다"를 듦 | 소극적 근거다. 더 무거운 근거는 블로킹 코드가 도는 스레드 위치다 | 근거가 약했음을 인정 | ADR-0021 |
| 사용자 | AI 가 "(가)와 (다)는 컨트롤러 코드가 같아 나중에 옮기면 된다" | 동시 조회가 몰리는 도메인이다. 가상 스레드가 없다는 전제의 코드가 병목이 되어 두 번 일하지 않나 | 스레드 수가 드러나지 않는 입장 제한이라 옮길 때 재설계가 필요함을 인정. 입장 제한은 Q23 | ADR-0021 |
| 사용자 | AI 가 (다) 가상 스레드에 계속 보수적 | (나)보다 위험도 비용도 낮은 (다)를 왜 꺼리나. DB 대기 낭비가 근거가 되지 않나 | 보수적이던 근거가 검증하며 약해졌음을 인정. 동시 DB 사용은 풀 크기가 정하므로 근거를 공급사 응답 대기로 정정 | ADR-0021 |
| 사용자 | AI 가 JDK 25 의 약점으로 설치 부담을 듦 | 설치는 당연한 일, 근거가 아니다 | 약점에서 뺌 | ADR-0022 |
| AI | 사용자는 21 의 지원 기간이 짧다고 읽음 | 지원이 끝난 건 non-LTS 24 이고 21 은 2028-09 까지다 | 지원 기간은 보조 근거로만 | ADR-0022 |
| AI | 사용자는 기존 ADR-0007 을 고치며 수정 기록을 남기자고 함 | 합의한 ADR-0001 규칙은 뒤집을 때 새 ADR | 새 ADR 로 대체하고 ADR-0007 에 대체 표시 | ADR-0022 |
| 사용자 | AI 가 연박 판정 질문을 "날짜별 재고를 기간 전체의 수 하나로 어떻게 합칠까요"로 던짐 | 무슨 말인지 모르겠다. 문장이 모호하다 | 3박 예시 표로 질문을 다시 씀 | — |
| 사용자 | AI 가 스펙상 날짜별 잔여 수뿐이라며 최솟값을 권함 | 정보를 이렇게밖에 못 얻나. 물리 객실을 알 방법은. 현업 데이터는 이것뿐이지 않을 것 같다 | 공개 개발 문서 조사. 현업엔 숙박일 수·체크인 불가 같은 제약이 더 오고, 최솟값 계산의 공식 근거는 없음 | ADR-0023 |
| 사용자 | AI 가 날짜 항목이 빠진 응답을 "공급사 오류가 매진과 구분되지 않는다"고 설명 | 그날 숙소 사정 같은 특수 상황인가. 공급사 오류인지 어떻게 아나 | 숙소 사정은 0 으로 오는 정상 매진. 원인은 알 수 없고 규약과 다르다는 사실만 안다고 정정 | ADR-0024 |
| 사용자 | AI 가 매핑에 없는 객실 타입처럼 그 객실 타입을 빼자고 권함 | 0 으로 보고 사실은 남겨 모니터링하자. 이전에 비슷하게 정한 방식대로 | 0 으로 판정하고 사실을 남김. 형태는 Q17·Q18 | ADR-0024 |
| 사용자 | AI 가 범위 밖·중복·음수 날짜를 판정으로 흡수하고 기록하자고 권함 | 엄격하게 보면 규격 외 응답은 에러로 처리할 수도 있다 | 우선 AI 안대로 정하고, 엄격한 실패 처리 여부는 다른 AI 에게 검토를 맡김 | ADR-0025 |
| 사용자 | AI 가 예약 불가 상품을 응답에서 빼자고 권함 (예약할 수 없는 항목은 쓸모없고 응답이 커짐) | 써 본 숙박 앱들은 매진으로 보여 준다. 빼지 않고 넘기는 게 로직도 단순하다 | 매진 표시는 화면의 결정이라 서버가 빼면 선택지가 사라짐. 응답 크기는 확인 안 한 우려. 0 으로 노출 | ADR-0026 |
| 검토 AI | AI 가 쓴 ADR-0024·0025 (값을 보정하고 기록) | 0 노출과 겹치면 규약 위반이 매진으로 보인다. ADR-0025 의 "0 이상 정수"는 스펙에 없다. 객실 타입 단위 선택지 누락, ADR-0015 와 같다는 서술도 틀림 | 사실 오류를 인정하고 객실 타입 단위 격리안을 검토 | ADR-0027 |
| 사용자 | 검토 AI 가 위반을 심각도로 나눠 처리하자고 함 | 오류라는 점은 같은데 심각도를 주관적으로 나눈 것 아닌가. 더 객관적으로 서술할 수 있나 | "응답을 읽을 수 있나 / 출력 계산에 쓰이나 / 값을 하나로 정할 수 있나" 세 질문으로 바꿔 ADR-0024·0025 대체 | ADR-0027 |
| 사용자 | AI 가 "구현 먼저냐 설계 먼저냐"를 무엇부터 구현할지의 순서 제안으로 답함 | 두 과정이 남기는 과정과 결과물이 어떻게 평가될지를 물은 것 | 두 과정의 산출물을 내 관점과 요구사항 문장 관점으로 다시 비교 | ADR-0028 |
| 검토 AI | AI 가 (가)의 약점으로 "결정이 자주 바뀌면 문서·코드가 어긋난다", "대체 ADR 이 많으면 사전 사고 부족으로 보인다"를 듦 | 코드 없이도 이미 어긋남이 있어 원인은 커밋 방식. 요구사항은 시행착오를 더 높게 봄. (가)도 검색 흐름은 Q1·Q2·Q5·Q6 에 막힘 | 약점을 "핵심 흐름 밖 설계가 밀림"으로 바꾸고 Verification 짝 규칙 추가. 어긋남은 확인해 보니 네 곳 | ADR-0028 |
| 스스로 | AI 가 가상 스레드 설정·웹 스타터·WebClient 스타터·Mock 모듈을 "기반 구성" 한 단위로 묶음 | 쓰는 코드가 없는 의존성을 미리 넣는 것이라 ADR-0006 을 어김 | 스타터와 설정은 그걸 쓰는 기능 단위로 옮기고 이번 단위는 Mock 모듈만 | ADR-0029 |
| 사용자 | AI 가 Verification 짝 규칙을 "이미 구현한 기능일 때"로 좁히자고 함 | 급하게 맞춘다는 규칙 자체가 이상하다. 나중에 한 번 전부 점검하면 된다 | 짝 규칙을 없애고 모아서 점검 | ADR-0029 |

### 구현 (9/15)

| 짚은 쪽 | 처음 주장 | 반박 | 결과 | 자세히 |
|---|---|---|---|---|
| 사용자 | AI 가 공급사 응답의 모르는 필드는 "Jackson 3 기본값이 무시라 설정 없이도 된다"고 설명 | 기본 동작이라도 명시적으로 설정하고 주석을 남겨야 라이브러리가 바뀌거나 다른 사람이 볼 때 의도를 알 수 있다 | 공급사 응답 DTO 마다 `@JsonIgnoreProperties(ignoreUnknown = true)` 와 이유 주석. 전역 설정은 우리가 받는 요청까지 관대해져 뺌 | Q6 |
| 사용자 | AI 가 응답 틀(`items`, `resultCode` 등)은 DTO 에서 엄격하게, 항목 필드만 받은 뒤 검사하자고 권함 | 외부 API 응답이지 우리 규약이 아니다. DTO 에서 잘라 버리면 공급사 장애를 일부 허용하려 해도 손쓸 수 없다. 판단이 들어간 로직은 DTO 의 역할이 아니고, 내부 로직에서 유지보수하는 편이 낫다 | DTO 는 받기만 하고, 틀까지 포함한 판정은 내부 로직에서 ADR-0027 기준으로. JSON 문법·타입 오류만 역직렬화 단계의 실패로 남음 | ADR-0030 |
| 사용자 | AI 가 공급사 A 스펙의 설명 표(`HotelCode`)와 응답 예시(`hotelCode`) 불일치에 별칭으로 두 표기를 모두 받자고 권함 | 설명 글이라 대문자로 쓴 것 아닌가. 굳이 대문자를 허용해야 하나 | 표의 나머지 필드와 응답 예시가 모두 소문자로 시작해 표기 실수로 봄. 별칭은 근거 없는 대비라 거둠. "JSON 은 보통 카멜케이스"는 일반론이라 근거에서 뺌 | ADR-0030 |
| 검토 AI | AI 가 어댑터를 기능별 경계 둘 + 공급사당 한 클래스로 권하며, 약점을 "공급사 둘이면 좁은 경계가 문제를 막는 일이 드물다"로 들고 묶는 인터페이스는 선택으로 둠 | 좁은 경계의 역할은 버그 방지가 아니라 소비자·테스트의 의존 모양. 검색이 목록 갱신을 부르지 않는다는 ADR-0014 를 타입으로 강제함. 기능 하나를 빠뜨리면 ADR-0019 때문에 공급사가 조용히 빠지므로 묶는 인터페이스는 필수 | 약점을 "어댑터가 어디까지 하는지 미정이라 클래스가 커질 수 있음"으로 옮기고 묶는 인터페이스 포함 | Q5 |
| 사용자 | AI 가 "두 경계를 구현한 클래스는 양쪽 List 에 주입되고 순서는 등록 순서"라고만 설명 | 필드를 하위 타입으로 선언해 받으면 되지 않나 | 구체 타입으로 받으면 공급사 추가 때 쓰는 서비스를 고쳐야 하고 공급사 클래스가 안쪽에 드러남. 경계 타입 List 로 받을지는 공급사 등록 방식 질문에서 | Q5 |
| 스스로 | AI 가 어댑터가 판정까지 맡는 안과 공통 로직에 두는 안을 비교 | 같은 판정 규칙(날짜 목록·최솟값)을 공급사마다 구현하면 공급사가 늘수록 어긋날 곳이 는다 | 어댑터는 공급사마다 다른 것까지, 공통 규칙은 공통 로직. ADR-0027 첫 질문만 어댑터에 남음 | Q5 |
| 사용자 | AI 가 내부 식별자 컬럼을 `uuid` 타입으로 권함 | 실무에서는 DB 를 바꾸기 어려운 자원으로 보고 검증을 줄여 varchar 로 두기도 한다고 들었다 | 그 관행은 CHECK·열거 타입·외래 키처럼 업무 규칙이 스키마에 박히는 곳에 맞고, 컬럼 타입은 값의 종류. 바꾸기 어려운 것은 발급된 식별자 값이지 컬럼 타입이 아님. 문자열→uuid 방향이 더 어렵다는 점까지 확인해 `uuid` 유지 | ADR-0034 |
| 사용자 | AI 가 매핑 테이블 예시에서 객실 타입 내부 식별자를 UUID 로 그려 보임 | 우리가 정한 적 없는데 왜 마음대로 UUID 인가 | ADR-0011 은 `(공급사, 공급사 코드)` 로 적힌 숙소 기준 문장이라 객실 타입은 미정이었음을 인정. 선택지를 다시 내고 무작위 UUID 로 합의 | 테이블 구성 ADR |
| 사용자 | AI 가 목록에서 사라진 숙소를 지우면 "같은 공급사 상품은 항상 같은 내부 식별자" 요구가 깨진다고 말함 | 그 문장은 동기화할 때마다 식별자가 섞이지 않게 하라는 뜻 아닌가. 사라졌다 돌아온 경우까지 포함한다고 볼 근거가 있나 | 과한 읽기였음을 인정. ADR-0011 도 매핑 데이터를 잃으면 식별자가 바뀐다는 점을 이미 받아들였음. 남기자는 근거를 요구사항이 아니라 "목록 API 에 상태 필드가 없어 일부 누락과 실제 제거를 구분할 수 없고 삭제는 되돌릴 수 없다"로 다시 세움 | 사라진 숙소 처리 ADR |
| 사용자 | AI 가 인터페이스를 "경계 타입"이라는 없는 용어로 부르며 설명 | 처음 듣는 말이다. 이상한 말을 지어내지 말라. 혼자 생각할 때도 마찬가지고, 동료와 소통하려면 서로 아는 말을 써야 낭비가 없다 | 조어임을 인정하고 ADR-0031·Feature Spec 의 표현을 "인터페이스"로 고침(9826cdc). 표준 용어는 인터페이스, 헥사고날에서는 포트 | ADR-0031 |
| 사용자 | AI 가 새 객실 타입 누락을 다른 어긋남과 구분되는 별도 종류로 남기자고 권함 | 대시보드로 보여줄 거면 어차피 나눠 집계한다. 종류를 지금 정할 필요가 있나 | 이번 결정은 빼고 남기는 것까지로 좁히고 분류는 Q17 로 | ADR-0015 |
| 사용자 | AI 가 기동 시 목록 실패를 앱을 켤 때마다 생길 수 있는 문제처럼 설명 | 처음 띄울 때를 빼면 매핑은 항상 있는 것 아닌가. 매핑이 뭐였지 | 매핑이 재시작 후에도 남는지가 미정이었음을 확인하고, DB 선택을 먼저 정함 | ADR-0017 |
| 사용자 | AI 가 "메모리 DB 에 두면 매핑이 빈다"고 말함 | 당연히 관계형 DB 라고 생각했는데 다른 의견이 있나. NoSQL 은 선택지가 안 되나 | 메모리 DB 는 H2 메모리 모드였고, 관계형 DB 는 요구사항이 정한 것이라 다른 형태로 대신할 수 없음을 설명 | ADR-0017 |
| 사용자 | AI 가 PostgreSQL 의 이점으로 재동기화 문법과 UUID 전용 타입을 듦 | 재동기화 문법이 특장점인가, MySQL 로는 불가능한가. 전용 타입은 식별자 형식을 바꾸면 컬럼 마이그레이션이 필요하다. 문자열 저장이 그렇게 손해인가 | 두 근거를 뺌. 선택 항목까지 넓혀 공식 문서로 다시 비교 | ADR-0018 |
| AI | 사용자가 "대규모 데이터에서 더 안정적인 마이그레이션"을 이유로 제시 | 확인된 건 실패한 스키마 변경의 롤백까지이고, 대규모 테이블 변경 때 잠금 동작은 확인하지 않았다 | 이유 문장을 확인된 범위로 좁힘 | ADR-0018 |
| 사용자 | AI 가 이유 문장에 "최근 더 익숙한 쪽은 MySQL"을 붙이고, "그 점을 택했다"로 씀 | 흐름이 이어지지 않는다. 점을 택한 게 아니라 그 점을 고려해 택한 것 | 익숙함은 잃는 것으로 옮기고 문장을 고침 | ADR-0018 |
| 검토 에이전트 | 조사 기록에 "식사 조건 23종", Airbnb "전 세계 기본", "foojay 플러그인이 있어야만", Kotlin "라인마다 18개월 지원"이라고 적음 | 출처를 다시 열어 대조하니 24종, "모든 사용자", 다운로드 저장소 설정이 필요하고 foojay 는 예시, JVM 표준 라이브러리 보안 지원 | 조사 기록과 ADR-0007 정정 | Day 3 |
| 사용자 | AI 가 공급사의 첫 목록 실패에 짧은 간격 재시도 장치를 권함 | 정말 드문 상황인데 고려해야 하는지부터 의문이다 | 이미 정한 기동 시 동기화와 주기 설정으로 회복 가능해 장치 없이 판단만 남김 | ADR-0019 |

### 패키지 구조 (9/16~9/17)

| 짚은 쪽 | 처음 주장 | 반박 | 결과 | 자세히 |
|---|---|---|---|---|
| 스스로 | AI 가 동기화 로그에 "사라짐표시" 건수를 내보냄 | 같은 목록을 다시 받은 공급사에 1건이 찍혔다. 세어 본 것은 반영을 시작하며 표시한 행 수였고, 뒤이어 되살아나므로 사라진 행이 아니었음 | 되살리기까지 끝난 뒤 남은 행을 세고 로그 이름을 "목록에없는숙소"로 고침 | ADR-0039 |
| 검토 AI | AI 가 패키지를 `supplier` / `catalog` / `search` / `config` 로 나누자고 함 | 저장소가 `catalog` 에 있으면 검색이 동기화 패키지를 보게 된다. 어댑터 인터페이스와 실패 신호도 소비자 쪽에 두면 반대편 소비자가 그 패키지를 보게 된다 | `mapping` 을 더하고 `config` 를 뺌. 인터페이스와 공통 형태는 `supplier` 로 | ADR-0040 |
| AI | 검토 AI 가 "`application.yml` 에 `stay.*` 가 없어 저장소만으로는 앱이 뜨지 않는다"고 지적 | 원문을 열어 대조하니 그 파일에 있고 실제로 기동된다. 인용한 소스 줄 번호도 파일 길이를 넘는 값이었음 | 그 지적만 버리고 나머지 지적은 반영. ADR-0040 Discussion 에 남김 | ADR-0040 |
| 스스로 | AI 가 파일 이동 커밋에 설정 클래스 파일 이름 변경까지 함께 담음 | `git add` 에만 경로를 주고 `git commit` 에는 주지 않아, 앞서 staged 된 변경이 섞였다. 메시지가 그 사실을 말하지 않음 | 아직 원격에 보내지 않은 로컬 커밋을 둘로 나눔 | Day 5 |
| 스스로 | 위와 같은 실수를 반복함 | 이번에는 경로를 지정했지만 한 파일에 성격이 다른 두 변경을 해 두고 파일 단위로 담았다. 메시지는 그중 하나만 설명함 | 이력을 되감아 둘로 나눠 다시 쌓음. 한 파일에 두 변경이 있으면 커밋을 먼저 나누고 고치기로 함 | Day 5 |
| 검토 AI | AI 가 동기화 실패 로그를 "공급사가 알린 실패"와 그 밖으로 나눔 | 우리 신호로 바뀌는 것이 공급사 B 의 결과 코드 하나뿐이라, A 의 HTTP 실패·타임아웃·읽을 수 없는 본문이 모두 "우리 쪽 결함" 가지로 간다. 어댑터가 차이를 흡수한다는 주석과 어긋남 | 어댑터가 실패를 한 신호로 감싸도록 고치고, 어댑터 테스트 세 건에 타입 단언을 넣음 | ADR-0027 |
| 검토 AI | AI 가 테스트 문자열에 널 문자를 넣음 | 실제 0x00 바이트가 들어가 git 이 이 파일을 바이너리로 보고, 이후 커밋의 diff 가 보이지 않는다 | 문자 그대로의 이스케이프로 교체 | Day 5 |
| 검토 AI | AI 가 ADR-0038 의 확인 항목을 코드 위치로 바꿈 | 코드 위치는 구현처이지 확인처가 아니다. 무응답 모드로 띄워 본 기록이 어디에도 없다 | 무응답 모드로 실제로 띄워 관찰하고 그 결과를 Verification 에 적기로 함 | ADR-0038 |
| 검토 AI | AI 가 ADR-0030 에 `{}` 응답 처리를 공급사 공통인 것처럼 적음 | B 는 결과 코드가 성공이 아니라 이미 실패로 본다. 빈 목록이 되는 것은 결과 코드만 성공이고 목록이 없을 때 | 공급사별 사실로 정정 | ADR-0030 |
| 스스로 | 커밋에 관련 없는 변경을 섞는 실수를 세 번째로 함 | 한 파일에 널 문자 제거·롤백 보강·새 테스트 세 가지를 해 두고 또 파일 단위로 담으려 했다 | 중간본을 만들어 단계마다 테스트를 돌리고 세 커밋으로 나눔 | Day 5 |
| 검토 AI | AI 가 실패를 감싸는 자리를 응답 변환 **뒤**에 붙임 | 그러면 공급사 응답을 우리 형태로 바꾸는 코드에서 난 오류까지 공급사 실패가 된다. 바로 앞 커밋에서 로그를 두 가지로 나눈 목적이 사라짐 | 감싸는 자리를 변환 앞으로 옮기고, 원인 클래스 이름을 메시지에 넣음 | ADR-0027 |
| 검토 AI | AI 가 설정 키를 지우면서 그 키를 확인처로 적어 둔 ADR 을 안 고침 | 동작이 바뀌는 커밋은 그것을 서술하는 문서 변경을 함께 담아야 한다 | ADR-0016 Verification 을 환경 변수까지 포함해 다시 씀 | ADR-0016 |
| 검토 AI | AI 가 롤백만 확인하고 저장 실패가 동기화 서비스를 지나는 경로는 두지 않음 | 실패 테스트가 모두 조회 단계에서 실패시킨다. 저장에서 실패했을 때 다른 공급사가 반영되는지는 확인되지 않음 | 통합 테스트 1건 추가 | ADR-0038 |
| 검토 AI | AI 가 관찰 기록에 기동에 걸린 시간을 판정 기준처럼 적음 | 기계마다 다른 수치다. 확인할 것은 기동 로그와 실패 로그의 순서다 | 재현 명령과 순서 기준으로 다시 씀 | ADR-0038 |
| 스스로 | 테스트 문자열에 널 문자를 또 넣음 (네 번째 반복) | 편집 도구를 거칠 때마다 이스케이프 표기가 실제 널 바이트로 바뀐다. 이번에는 커밋 전에 검사해 잡음 | 같은 방법으로 교체하고, 이 파일을 고친 뒤에는 널 바이트 검사를 먼저 하기로 함 | Day 5 |
| 검토 AI | AI 가 널 문자 실수를 적은 기록 자체에 널 바이트를 넣어 커밋함 (다섯 번째) | JOURNAL 776·798행, 이스케이프 표기를 백틱으로 감싼 그 자리다. git 은 파일 앞부분만 보고 이진 판정을 해 텍스트 diff 로 들어갔다 | 제거하고, 저장소의 텍스트 파일 전체를 훑어 남은 것이 없음을 확인 | Day 5 |
| 검토 AI | AI 가 커밋 `확인:` 에 적은 테스트 이름을 ADR Verification 에 옮기지 않음 | 커밋 규칙이 같은 커밋에서 하라고 정해 둔 것이다 | ADR-0027 Verification 에 감싸기 범위 테스트 3건 기재 | ADR-0027 |
| 검토 AI | AI 가 감싸기 자리를 옮긴 뒤 조건과 주석을 그대로 둠 | 옮긴 뒤로는 그 위에서 공급사 실패가 올 경로가 없다. "어댑터가 이미 바꾼다"는 순서상 틀린 말이 됐고, 조건 하나는 지나갈 길이 없어졌다 | 주석을 사실에 맞게 고치고 조건을 남긴 이유를 적음. 조건을 지울지는 판단 대기로 올림 | ADR-0027 |
| 사용자 | AI 가 "이 테스트가 회귀를 잡는지"라고 씀 | 번역체 아니냐 | "회귀 테스트"는 실무 용어지만 앞 글자만 떼어 명사로 쓰면 다르게 읽힌다. ADR-0007 의 두 곳도 "전에 되던 것이 다시 깨진 항목"으로 바꿈 | Day 5 |
| 사용자 | AI 가 위 지적을 받아들이고도 이어지는 설명에서 "회귀"를 계속 씀 | 그게 지금 무슨 말인데 맥락을 모르겠다 | 지적받은 것을 그 자리에서 다시 어겼다. 앞으로 "전에 되던 게 이번에 깨졌다"로 쓰기로 함 | Day 5 |
| 사용자 | AI 가 검증을 정적 팩토리가 성공이나 사유를 돌려주는 쪽으로 권함 | "값을 빠뜨리는 게 늘 있는 일"이 어디서 나왔나. 검증되지 않은 값이 돌아다녀야 할 상황이 있나. 정팩메를 두더라도 생성자를 부르는 형태로 해 왔다 | 빈도 근거가 없었다. ADR-0037 이 두 곳에서 "빈도 수치는 어디에도 없다"고 적어 뒀는데 AI 가 반대를 단언했다. 불변식을 생성자에 두기로 정함 | ADR-0041 |
| 검토 AI | AI 가 없는 최대 수용 인원을 0 으로 바꿔 생성자에 넘김 | 값이 없는 것이 "1 미만이다"로 기록된다. 전에는 구분됐다. 게다가 AI 가 쓴 테스트가 그 틀린 사유를 정답으로 고정했다 | 정적 팩토리가 없는 값을 따로 거부해 사유를 구분 | ADR-0041 |
| 검토 AI | AI 가 중복 검사를 생성자보다 먼저 하도록 순서를 바꿈 | 코드가 없는 항목들이 한 그룹으로 묶여 "같은 코드가 두 번 왔다"가 된다. 코드가 없는 두 숙소는 같은 코드가 아니다 | 코드가 없으면 항목마다 따로 보게 고치고 테스트 추가 | ADR-0041 |
| 사용자 | AI 가 ADR-0027 판정 표의 처리 칸을 "무시한다", "부분 실패로 표시한다" 로 적음 | 무엇을 어떻게 하는지가 빠져 모호하다 | 주어와 목적어를 넣어 문장으로 고침. "원본을 격리해 남긴다"는 하지 않는 일을 하는 것처럼 적은 것이라 함께 정정 | ADR-0027 |
| 검토 AI | AI 가 ADR 의 인용 85건 중 열여덟 곳을 틀리거나 근거 없이 적음 | Flyway 인용은 원문의 절반만 따와 스타터만으로 되는 것처럼 읽힌다. Kotlin 문서는 "테스트하는"이 아니라 "완전히 지원하는"이다. 배포 점유율·관행의 실재 등은 출처가 없다 | 틀린 인용 넷을 고치고, 근거 없는 단언 다섯은 확인된 범위로 좁히거나 판단임을 밝힘. 밀리는 줄 번호는 파일·이름으로 바꿈 | Day 5 |
| 사용자 | AI 가 번역어 "소비자" 를 설명 없이 씀 | 그게 함수 호출하는 곳을 말하는 거냐. 간단한 영어 용어는 그대로 써라. 억지 번역은 성장에도 이해에도 도움이 안 된다 | 저장소 20곳을 `consumer` 로 바꿈. 테스트 이름 하나와 ADR-0031 Verification 의 참조도 함께. 공정거래위원회 문서 이름은 그대로 | ADR-0031 |
| 사용자 | AI 가 식별자가 두 곳에서 와 "다른 공급사 행이 통째로 사라짐 표시될 수 있다"고 함 | 그게 다른 공급사를 가리킬 일이 있나 | 두 값이 같은 상수에서 나와 실행 중 갈릴 경로가 없었다. 검토자는 유지보수 냄새로 짚었는데 AI 가 살아 있는 결함처럼 옮겼다. 무게를 낮추고 검색 단위로 넘김 | ADR-0031 |

## Day 0 (2026-09-13)

### 사건: 승인 없이 진행한 것을 되돌림

- **무슨 일**: 작업 방식을 정하기 전에 AI 가 요금 모델·실패 분류·타임아웃
  값·패키지 구조를 스스로 정하고 파일 15개를 만듦
- **문제**: 갈림길에서 멈추지 않고 진행함
- **조치**: 발견 즉시 저장소를 커밋 0개로 되돌림. 코드는 사본만 남기고 버림
- **왜 되돌렸나**: 몇 시간 분량을 잃더라도, 결정이 누구 것인지 불분명한
  채로 계속 쌓는 것보다 낫다고 판단

### 결정

| 결정 | 내용 |
|---|---|
| 작업 방식 | 갈림길마다 AI 가 선택지 + 잃는 것 + 추천 + 약점을 내고 사람이 결정 |
| 자율 실행 | 없음 |
| 기록 | ADR 로 남김 (ADR-0001) |

## Day 1 (2026-09-14)

### 정한 것

| # | 결정 | ADR |
|---|---|---|
| 1 | ADR 형식 확정 (결정 하나-파일 하나, AI 추천 표시, Discussion 절) | 0001 |
| 2 | 언어 = Kotlin | 0002 |
| 3 | CLAUDE.md 저장소 공개 | 0003 |
| 4 | 커밋 규약 | 0004 |
| 5 | Mock 공급사 = 별도 Gradle 모듈 | 0005 |
| 6 | 의존성은 기능 만들 때 추가 | 0006 |
| 7 | 빌드 도구·프레임워크 버전 | 0007 |

### 뒤집힌 것들

**언어(Kotlin)**
```
AI 안    → Java 권함. "AI 가 만든 Kotlin 관용구를 인터뷰에서 방어하기 어렵다"
반박     → 설명 가능성을 구현 기준으로 두면 지금 이해도가 코드 품질의 상한이 됨.
           모자란 건 공부하면 됨
검증     → 근거를 빼면 남는 건 2년 공백뿐. 10개월·1만 줄 경험에 비추어 결정적이지 않음
결과     → AI 가 Kotlin 으로 추천 변경
```

**Mock 배치 (`src/test` → 별도 모듈)**
```
AI 안    → src/test 에 두면 모듈 안 늘리고 컴포넌트 스캔도 회피
재검토   → src/test 클래스패스로 띄우면 앱의 main 리소스·의존성을 그대로 물려받음.
           H2 파일 모드에서 앱과 Mock 이 같은 DB 파일을 열려다 락 충돌
결과     → 별도 Gradle 모듈로 변경 (ADR-0005)
```

**Gradle 버전 (9.4.1 → 9.7.1)**
```
AI 안    → "9.4.1 이미 검증했으니 유지"
반박     → "9.4.1 이 9.7.1 보다 나은 점이 뭐냐" → 답 못 함. 관성이지 근거가 아니었음
검증     → 실행 JDK 지원 범위 동일(17–26). 9.7.1 에 버그 수정 포함, 실측 확인
결과     → 9.7.1 로 변경
```

**Kotlin 버전 근거 (2.4.20)**
```
AI 안    → "최신이고 Boot BOM 과 안 어긋남"
반박     → Gradle 에서 이미 기각된 "최신이라서" 논리와 같음
검증     → Gradle 9.7.1 을 지원 범위에 담는 KGP 버전은 2.4.20 이 유일
결과     → 근거만 교체 (결정은 동일)
```

**Boot 3→4 전환 근거**
```
AI 안    → "webflux 스타터 + web-application-type: servlet 설정 필요"
검증     → 틀림. Boot 3 도 webmvc+webflux 공존 시 자동으로 SERVLET 판정
정정     → 진짜 단점은 안 쓰는 리액티브 서버 의존성이 딸려오는 것
```

**JDK 자동 다운로드**
```
AI 안 1  → "JDK 21 없으면 빌드 실패"
AI 안 2  → (스스로 뒤집음) "Gradle 이 자동으로 받아온다"
재확인   → 둘 다 틀림. 그 JDK 는 몇 달 전 다른 프로젝트로 이미 캐시에 있던 것.
           foojay-resolver-convention 플러그인 없이는 자동 다운로드 안 됨
```

**저장소·패키지 이름**
```
AI 안    → dev.goohong 권장. 근거: GitHub 핸들이 9rrong(숫자 시작이라 패키지 불가)
검증     → 실제 핸들은 goohong (gh api 로 확인). 전제가 틀림
편향 확인 → 이 대화를 모르는 별도 세션에 같은 질문 재확인 → 패키지 추천이 실제로 바뀜
결과     → com.stayaggregator (기존 프로젝트들과 같은 방식)
```

### 그 외 거부한 것

| AI 제안 | 거부 이유 |
|---|---|
| "설명 못 하면 더 단순한 코드로 다시 쓴다"를 규칙으로 | 그러면 지금 이해도가 구현 가능한 코드의 상한이 됨. 이해 못 하는 건 나중에 공부하면 됨 |

### 반성

- AI 활용 기록을 하루 종일 미룸. `docs/ai-log.md` 를 따로 만들었다가
  JOURNAL 과 역할이 겹친다는 지적을 받고 다시 합침

### 다음에 할 것

표준 모델(금액, 숙박 구간, 재고, 판매 조건) 설계.

## Day 2 (2026-09-14, 계속)

### 문서 구조 재정비

**측정**: ADR 8개, 문서 927줄 vs 코드 11줄

**진단**: ADR 8개 중 4개(0001·0003·0004·0006·0008)가 도메인 판단이 아니라
작업 방식 자체에 관한 메타 ADR. 앞으로는 도메인 판단(요금·실패분류·타임아웃)만
ADR 로 남기기로 함.

**Feature Spec 도입** (ADR-0008)
```
처음 이해    → "ADR 이 헌법, Feature Spec 이 법률" (위계 관계로 오해)
정정         → 위계가 아니라 관점 차이. Feature Spec = 무엇을(What),
               ADR = 왜(Why). Feature Spec 은 관련 ADR 을 모아 보는 인덱스
```

### 확장성 원칙 미반영 발견

- **문제**: 첫날 정한 "공급사가 늘어날 걸 전제로 확장에 열어두기"가 코드에
  전혀 반영 안 됨. 요금 필드를 정하려는 시점에 A, B 만 알고 스키마를 짜면
  공급사가 늘 때마다 표준 모델을 계속 고쳐야 하는 문제로 드러남
- **해법**: 어댑터 패턴. 표준 모델은 "지금 응답이 필요로 하는 것"만 최소로,
  공급사별 원본은 각 어댑터가 sealed interface 로 보존. 새 공급사 = 어댑터
  구현체 하나 추가, 표준 모델은 안 바뀜
- **헷갈릴 뻔한 것**: 컴포짓 패턴(루티의 `PlaceSearcherComposite`)과 비교함

  | | 루티 Composite | 우리 필요 |
  |---|---|---|
  | 동작 | 여럿 중 쓸 수 있는 하나만 골라 씀 | 여럿을 전부 병렬로 불러 합침 |
  | 패턴 | 폴백 체인 | 어댑터 + fan-out |

### Reactor vs 코루틴 (ADR-0009)

```
AI 안    → "이 규모(공급사 2~3개)에선 성능 차이 없다" (요구사항의 지정 취지만 근거)
반박     → 이 도메인은 공급사가 많아질 걸 전제해야 함. "숙소 수천 개" 경고를
           스스로 무시한 판단이었음
재검증   → 수천 개 규모에선 동시 호출 수 제한이 실제로 필요.
           Reactor = flatMap(mapper, concurrency) 한 줄 / 코루틴 = Semaphore 직접 구현
결과     → 결정(Reactor)은 유지, 근거만 "요구사항 취지" → "수천 개 규모의 실질 필요"로 교체
```

### 다음에 할 것

Q1(금액 표현)을 어댑터 구조 위에서 구체화. `NormalizedRate` 와 `RateAdapter`
인터페이스를 실제 코드로 작성.

## Day 2 밤 (2026-09-14)

### 공개 저장소 점검과 히스토리 다시 쓰기
- **발견**: 문서에 이 저장소에서 쓰지 않기로 한 표현과, 요구사항 문서의 문장을 따옴표로 거의 그대로 옮긴 곳이 있었다. 한 ADR 을 커밋할 때 그 파일만 검사를 빼먹은 것이 시작이었다
- **조치**: 현재 파일을 고친 뒤, 과거 커밋까지 다시 써서 강제 푸시했다. 커밋 시각은 원래 기록 그대로 두었다
- **재발 방지**: 푸시 전 훅으로 금지 표현이 든 커밋은 푸시를 막는다
- **과하게 적용한 것**: 이어서 요구사항 문서와 몇 글자만 겹쳐도 문장을 고치다가, "이 작업의 성격이 드러나지만 않으면 괜찮다, 목적에서 벗어난 행위를 위한 행위"라는 지적을 받고 멈췄다

### 요구사항 문서를 부분만 읽고 논의한 것
```
문제     → 요금 모델을 몇 시간 논의하는 동안 요구사항 문서 3장을 필요한 절만 골라 읽었다
지적     → "가장 중요한 문서인데 아직 안 읽었다는 건 말이 안 된다. 최소 3회독"
조치     → 원본 전체 읽기 → 요구사항 목록 뽑기 → 원문과 한 줄씩 대조
새로 본 것 → 정렬·페이징은 범위 밖 / 서버 방식(WebFlux·MVC)을 고른 이유를 남겨야 함 /
            숙소 코드 50개 초과는 오류 / 숙소명이 목록·조회 두 API 에 다 있음 /
            같은 객실인데 공급사마다 객실명이 다름
결과     → Feature Spec 오픈 질문을 5개에서 13개로 늘림
```

### 도메인 조사
- 규제(한국·미국), 플랫폼 화면 세 곳, 업계 연동 규격 세 가지를 조사해 `docs/research/rate-display.md` 에 남겼다
- 조사로 확인한 것: 총액이 기준값이다 / 1박 가격의 뜻은 플랫폼마다 다르다 / 조식과 무료 취소가 요금 조건으로 공통으로 쓰인다

### "보존"을 잘못 옮긴 것
```
사용자 말 → 외부 응답은 보존해야 한다
AI 해석  → 원본을 저장·기록한다 (로그? DB? 표준 모델에 원본 필드?)
실제 뜻  → 공급사 응답 DTO 는 스펙의 모든 필드를 받아야 한다. 저장까지는 아니다
결과     → 원본 저장 논의 몇 차례가 헛돌았다. Q6 으로 올림
```

### 요금·재고는 저장하지 않는다
- 헷갈린 지점: 요금 표준 모델을 논의하다 보니 요금을 저장하는 것처럼 들렸다
- 정리: DB 에는 공급사 코드와 내부 식별자 매핑만 둔다. 요금·재고는 검색할 때마다 받아서 메모리에서만 표준 모델로 바꾸고 응답한 뒤 버린다

### 병합해도 내부 식별자 유지 (ADR-0010)
```
AI 안    → 내부 숙소 테이블과 매핑 테이블을 나눠야 병합할 자리가 생긴다
반박     → 행 두 개면 되지 않나? → 기본 키·인덱스까지 내려가자 "ERD 가 아니라 결정을 먼저"
검증     → 한 테이블로도 병합 가능. "자리가 없다"는 틀린 가정. 테이블 구조는 결정 뒤의 설계
결정 질문 → 병합하면 이미 발급한 내부 식별자를 바꿀 것인가
결과     → 바꾸지 않고 묶음 정보를 덧붙인다. 근거는 사용자가 든 "예약 대상은 공급사 상품이다"
```

### AI 가 합의한 방식을 깬 것
- 방향 의견을 요청받자 AI 가 남은 시간을 근거로 "정하자마자 구현", "선택 항목은 설계만"을 제안했다
- 처음에 시간을 제약으로 두지 않기로 했고, 작업 순서와 범위도 합의했었다. 지적받고 거둬들였다

### 멈춘 곳
Q13 내부 식별자 형태를 여쭤본 상태에서 멈췄다. 답은 아직 없다.
- (가) 순서대로 번호 발급 / (나) 무작위 식별자(UUID) 발급 ← AI 추천 / (다) 공급사와 공급사 코드로 계산
- AI 추천의 약점: 로컬 DB 를 새로 만들 때마다 식별자가 바뀐다. 이 상황에서 같은 값을 보장하는 건 (다) 뿐이다

### 다음에 할 것
Q13 결정 → Q8 (숙소명·객실 타입명·최대 수용 인원을 매핑과 함께 저장할지) → Q10 → Q11 → Q12

### 참고 자료
- [요금 표시와 요금 조건 조사](docs/research/rate-display.md)
- [빌드 도구·프레임워크 버전 조사](docs/research/build-versions.md)

## Day 3 (2026-09-15)

### 내부 식별자 형태 (ADR-0011)
```
AI 안 1  → 무작위 UUID 발급 (순서·개수가 안 드러남)
AI 안 2  → 공급사 코드로 계산으로 변경 (데이터를 잃어도 같은 값이 나온다는 이유)
반박 1   → 트레이드오프가 그뿐인가 → 역추적 위험, 입력 표기 고정, 같은 상품 중복 불가를 추가로 찾음
반박 2   → 비밀 키 계산 성능은? → 로컬 측정: HMAC 약 136ns, 무작위 UUID 약 137ns. 부담 없음
반박 3   → DB 를 엎는 상황까지 유지해야 하나, 마이그레이션이 있지 않나
검증     → 마이그레이션·백업은 발급 방식과 무관하게 저장값을 옮김. 계산 방식의 이점은 데이터 유실 후
           재구성이라는 드문 사고 대비뿐이고, 로직을 못 바꾼다는 제약은 그 이점의 뒷면
결과     → 무작위 UUID 를 발급해 저장. 사용자는 영향이 크지 않다고 보고 기본 방식을 택함
```

### 응답의 정적 정보 출처 (ADR-0012)
```
AI 안    → 목록 기준 저장값 + 최대 수용 인원만 조회값(예외) + 어긋나면 갱신 신호
중간 혼선 → AI 가 흐름 설명 없이 "새 객실 타입이 조회에 먼저 나온다"는 시나리오를 제시해 사용자가 되물음.
           이어서 정하지 않은 저장 구조를 정해진 것처럼 말해 한 번 더 되물음
정리     → 조회는 숙소 코드로만, 객실 타입은 응답으로 받음(요구사항). 이 흐름 위에서 시나리오 11개를 다시 봄
반박     → 매핑이 핵심인데 필드 하나에 예외를 두는 느낌. 최대 인원은 드물게 바뀜.
           바뀐 정보는 공급사가 갱신을 요청하는 방식을 나중에 두면 됨
결과     → 예외 없이 목록 기준. 최대 인원 증가 시 잠깐의 모순과 새 객실 타입 누락은 알려진 한계로,
           공급사 알림 방식은 이번 스펙에 없어 범위 밖 확장으로 기록. 갱신 신호는 Q10 에서
```

### 숙소 목록 호출 시점 (ADR-0013)
- **숙소 목록을 앱 기동 시와 일정 주기마다 받아옴**: 드물게 바뀌는 목록은 드물게, 매번 바뀌는 재고·요금은 매번. ADR-0012 의 알려진 한계가 주기 안에서 끝남
- AI 추천과 같았고 반박은 없었다
- 새로 생긴 질문을 오픈 질문에 올림: Q14 주기 값과 근거 / Q15 어긋남을 보면 갱신을 앞당길지 / Q16 새 객실 타입 누락 처리

### 알게 된 것
- 푸시 전 훅이 JOURNAL 의 일반 단어 하나를 두 번째로 잡았다. 걸리면 무엇이 걸렸는지 사용자에게만 알리기로 함

### 목록 어긋남 처리 (ADR-0014)
```
혼선     → 사용자가 가격·재고를 저장했다가 다시 쓰는 흐름이 있었는지 헷갈려 함.
           이어서 이름·최대 인원을 왜 미리 저장했는지 되물음
정리     → 목록 동기화와 검색마다의 재고·요금 조회라는 두 흐름을 요구사항 기준으로 다시 설명.
           코드·식별자 저장은 요구사항상 필요, 이름·최대 인원 저장은 ADR-0012 의 선택
AI 안    → 어긋남을 기록만 하고 갱신은 주기대로
망설임   → 알면서 다음 주기까지 두는 게 맞나 → 검토 AI 에게 물음
검토 AI  → 결론 같음. 다만 본질은 "고객 검색이 목록 갱신을 일으켜도 되는가". 이 스펙은 숙소 하나만
           다시 받을 수 없어 교정 비용이 크고, 매출에 닿는 어긋남은 새 객실 타입 누락뿐
결과     → 검색에서 감지, 교정은 동기화 주기에서만. 새 객실 타입 누락의 무게는 Q16, 어긋남 정의와
           남기는 양은 Q17, 주기 값과 운영자 설정은 Q14 로 넘김
```

### 매핑에 없는 객실 타입 처리 (ADR-0015)
```
혼선     → 조회를 객실 타입 코드로 하는 줄 알아 "매핑에 없는 게 올 수 있나" 되물음
정리     → 공급사 API 두 개와 우리 검색 API 를 누가·언제·무엇을 넣고 받는지로 다시 정리.
           재고·요금 조회에는 숙소 코드만 들어가서 공급사가 지금 가진 객실 타입이 옴
AI 안    → 응답에서 빼고, 다른 어긋남과 구분되는 별도 종류로 남김
사용자   → 드문 상황을 덮느라 일반 검색 성능이 떨어지면 안 되니 빼는 것에 동의.
           다만 대시보드로 보면 어차피 나눠 집계하니 종류를 지금 정할 필요는 없다
결과     → 빼고, 뺀 사실을 남긴다. 분류는 Q17 에서
```

### 순서 조정
- 어긋남의 정의와 분류(Q17)는 공급사별 성공률·지연·타임아웃과 같은 기준으로 나눠야 해서, 연동 지표·모니터링 설계(Q18)에서 함께 정하기로 함

### 숙소 목록 갱신 주기 (ADR-0016)
```
AI 안    → 기본 하루 한 번 + 운영자 설정 변경 가능. 근거는 해외 공급사 두 곳의 원문 안내
사용자   → 동의. 다만 더 조사할 곳은 없었나, 국내 업체도 보라
추가 조사 → Agoda·ETG 도 하루 한 번. 둘은 변경분만 매일 받는 방식을 따로 둠.
           국내 연동 플랫폼 한 곳은 주기 대신 웹훅으로 변경 사실만 알리고 다시 조회하게 함.
           다른 한 곳은 전체 목록 조회만 있음
막힌 곳  → ETG 안내 페이지 하나가 사라져 "전체 덤프는 매주"는 확인 못 함. WebBeds·TBO·국내 채널 매니저 일부는 공개 문서를 못 찾음
결과     → 결정 그대로. 근거를 네 곳으로 보강하고, "변경분 조회가 없어 매일 전체를 받는다"는 약점과 알림 방식 확장 방향을 기록.
           설정 주기의 허용 범위는 Q19 로
```

### 참고 자료
- [숙소 콘텐츠 갱신 주기 조사](docs/research/catalog-sync-interval.md)

### 매핑 저장 DB (ADR-0017)
```
혼선     → 기동 시 목록 실패(Q11)를 AI 가 매번 생길 문제처럼 설명하자, 사용자가 "처음 이후엔 매핑이 항상 있지 않나" 되물음
원인     → 매핑이 재시작 후에도 남는지(DB 와 데이터 유지 방식)가 미정이었음
정리     → 관계형 DB 는 요구사항이 정함. NoSQL 로 대신할 수 없고, 선택 항목에 다른 저장소를 더하는 건 그 항목에서
비교     → H2 메모리 / H2 파일 / 서버형 DB / H2 파일 + 설정으로 서버형. H2 동작은 공식 문서로 확인
결과     → 서버형 관계형 DB. Q11 은 맨 처음·새 공급사의 첫 목록으로 좁혀짐. PostgreSQL 과 MySQL 중 무엇은 Q21
```

### PostgreSQL 채택 (ADR-0018)
```
AI 안    → PostgreSQL. 근거는 UUID 전용 타입, 재동기화 문법
반박     → 재동기화는 특장점이 아니다. 전용 타입은 식별자 형식을 바꾸면 묶인다. 문자열 저장 손해가 큰가
검증     → 두 근거는 빠짐. 선택 항목까지 넓혀 확인하니 스키마 변경 롤백·문자열 유사도·JSON 인덱스·기본 격리 수준이 다름
사용자   → 유사도는 앱에서 할 것 같아 차이가 작다. "대규모 데이터에서 안정적인 마이그레이션"으로 PostgreSQL
정정     → 확인된 건 실패한 변경의 롤백까지. 대규모 잠금 동작은 미확인이라 이유를 좁힘. 익숙함(MySQL)은 잃는 것으로
결과     → PostgreSQL
```

### 근거 표기 방식
- 사용자가 근거에 공식 문서 링크와 문서 안 위치까지 넣어 달라고 함. 내부 문서도 같은 방식
- 별도 에이전트가 조사 기록 세 개와 ADR 의 출처를 다시 열어 문장·앵커를 대조함. 사실 정정 다섯 건(식사 조건 24종, Airbnb "모든 사용자", FTC 시행일의 출처, Kotlin 보안 지원 범위, JDK 자동 다운로드 조건)
- **정정**: Day 1 기록의 "foojay-resolver-convention 플러그인 없이는 자동 다운로드 안 됨"은 과장이었다. 툴체인 다운로드 저장소를 설정 플러그인으로 추가해야 하고, foojay 는 그 대표적인 예다. 그날의 이해를 남기려고 그 줄은 고치지 않았다

### ADR 문체
- ADR 을 처음 제안한 글이 Decision 을 능동태의 완전한 문장으로 쓰라고 해서, Decision 절 이유 문장의 과거형을 현재형으로 맞춤. 지난 경험을 가리키는 문장과 Discussion 은 과거형

### 첫 목록 실패 (ADR-0019)
- 매핑이 DB 에 남게 되면서 문제는 "맨 처음, 새 공급사의 첫 목록"으로 좁혀짐
- AI 는 짧은 간격 재시도를 권했으나, 사용자가 드문 상황을 고려할 필요부터 의문을 제기
- 재시작(기동 시 동기화)과 주기 설정으로 회복할 수 있어 장치를 만들지 않고 판단만 남김. README 에 Mock 먼저 띄우기

### 서버 방식 (ADR-0020)
- 요구사항은 WebFlux 전면 도입을 요구하지 않고 이유만 남기라고 함
- 오래 기다리는 곳은 공급사 호출이고 그 부분은 MVC 에서도 WebClient 로 논블로킹. WebFlux 는 밀리초 단위 매핑 조회까지 R2DBC 로 바꾸는 비용
- 편향을 덜려고 다른 AI 에게 따로 검토를 맡겼고 같은 결론(MVC + WebClient)

### 요청이 공급사 응답을 기다리는 방식 (ADR-0021)
```
배경     → Reactor 와 코루틴의 제어권 전달, Future 와 Mono, subscribe, 가상 스레드 동작을 공식 문서로 짚고 넘어감
AI 안    → (나) Mono 반환 → 요구사항 문서 재대조 후 (가) block + 플랫폼 스레드
사용자   → (가)는 아니다. 동시 조회가 몰리는 도메인이고, 가상 스레드 없는 전제의 코드가 나중에 병목이 된다
검토 AI  → (가) 선택. 다만 "동시 요청 요구가 없다"는 약한 근거이고, (나)는 응답 뒤 DB 호출이 조용히 이벤트 루프를 막는다
검증     → (나)의 약점 넷(이벤트 루프 블로킹 미검출, ThreadLocal 끊김, 필터 미호출, 30초 비동기 타임아웃) 모두 원문 확인.
           (다)의 실제 위험은 pinning 이 아니라 HikariCP 7.0.2 스핀(7.1.0 에서 수정)
사용자   → 왜 (다)에 보수적인가. DB 대기 낭비가 근거가 되지 않나
정정     → 보수적이던 근거는 하나씩 약해졌음. 동시 DB 사용은 풀 크기가 정하므로 가상 스레드가 줄이는 건 공급사 응답 대기
결과     → (다) block + 가상 스레드. 입장 제한·풀 크기·요청 전체 시한은 Q23
```

### JVM 25 (ADR-0022)
- 가상 스레드를 쓰면서 Boot 가 강력히 권장하는 24 이상을 벗어나지 말자는 사용자 판단으로 21 대신 25
- 24 가 아닌 25 인 이유를 더 모음: LTS, Boot 관리 Netty 가 25 부터 Unsafe 를 기본으로 쓰지 않아 JDK 24 의 경고를 피함
- 기존 ADR 을 고치는 대신 ADR-0001 규칙대로 새 ADR 로 대체

### 연박 예약 가능 객실 수 (ADR-0023)
```
AI 안    → 날짜별 잔여 수의 최솟값 (3/1/5 → 1, 하루라도 0 이면 0)
사용자   → 질문이 모호하다 → 3박 예시로 다시 물음
사용자   → 정보가 이것뿐인가, 물리 객실을 알 수 있나, 현업 데이터는 더 풍부할 것 같다
조사     → 공개 개발 문서(Booking.com, Google Hotel Prices, Hotelbeds, Expedia Rapid, Mews). 핵심 인용은 원문에서 다시 대조
알게 됨  → 올려 보내는 API 는 객실 타입 × 날짜 단위, 검색해 가는 API 는 공급사가 기간 전체를 계산.
           "모든 날짜가 가능해야 한다"는 공식 문장은 있고 최솟값 계산은 없음. 현업엔 숙박일 수·체크인 불가·판매 중지 같은 제약이 더 옴
사용자   → 포인트가 될 수 있는 지점이라 더 고민할 점으로 남기고, 스펙 안에서 가능한 최소로 (가)
결과     → 최솟값. 날짜 목록 불일치 처리는 Q24
```

### 참고 자료
- [연박 재고 표현 조사](docs/research/multi-night-inventory.md)

### 빠진 숙박일 (ADR-0024)
```
AI 안    → 날짜 항목이 빠진 응답이면 그 객실 타입을 빼고 사실을 남김
사용자   → 그날만 방이 없는 특수 상황인가, 공급사 오류인지 어떻게 아나
정정     → 방이 없는 날은 0 으로 오는 정상 매진. 이건 항목 자체가 없는 규약 위반이고 원인은 응답만으로 모름
사용자   → 0 으로 보고 사실을 남겨 모니터링. 어긋남·매핑 누락을 다룬 방식대로
결과     → 빠진 날은 0. 기록 형태는 Q17·Q18. 범위 밖·중복·음수는 Q24 에 남음
```

### 범위 밖·중복·음수 날짜 (ADR-0025)
- 빠진 날에서 정한 원칙("모르는 것은 팔지 않는 쪽으로, 규약과 다른 응답은 사실을 남김")을 나머지 세 경우에 적용
- 범위 밖 날짜는 무시, 중복은 작은 값, 음수는 0. 음수를 0 으로 보는 방식은 Google Hotel Prices 재고 문서와 같음
- 사용자가 "규격 외 응답은 엄격하게 에러로 볼 수도 있다"고 짚음. 우선 이대로 정하고 엄격한 처리 여부는 다른 AI 에게 따로 검토를 맡긴 뒤 넘어감

### 예약 불가 상품 노출 (ADR-0026)
```
AI 안    → 응답에서 뺌. 예약할 수 없는 항목은 쓸모없고, 페이징이 없어 응답이 커짐
사용자   → 써 본 숙박 앱은 매진으로 보여 줌. 빼지 않고 넘기는 게 단순함
검증     → 매진 표시 여부는 화면이 정할 일. 서버가 빼면 그 선택지를 없앰. 응답 크기 우려는 측정 안 함.
           "대부분의 앱이 그렇다"는 확인 안 한 일반화라 근거에서 뺌
결과     → 0 으로 노출. README 에 한 줄
```

### 규약 위반 처리 기준 (ADR-0027, 0024·0025 대체)
```
검토 AI  → 공급사 응답 전체 실패는 정상 항목까지 버리고 재시도해도 같음. 객실 타입 단위로 빼고 원본 격리를 권함.
           근거로 RFC 9413(엄격함은 고칠 수 있는 쪽에 알리기 위함), Booking.com·Google 의 항목 단위 거부
대조     → 인용은 모두 원문과 일치. 다만 Google 의 중복 규칙은 인원 요소에 대한 것이라 날짜 중복 선례에서 뺌
짚은 점  → 0 노출(ADR-0026)과 겹쳐 규약 위반이 매진으로 보임. ADR-0025 의 "0 이상" 서술은 스펙에 없음
사용자   → 접근은 좋지만 심각도 구분이 주관적. 객관적으로 서술할 수 있나
정리     → 세 질문을 순서대로: 응답 전체를 읽을 수 있나(아니면 공급사 실패) / 계산에 쓰이나(아니면 무시) /
           값을 하나로 정할 수 있나(아니면 그 항목 제외 + 격리). 음수만 필드 정의에서 끌어낸 해석
결과     → ADR-0027 로 0024·0025 대체. 공급사 상태에 세 번째 값이 필요한지는 Q25
```

### 구현과 설계의 순서 (ADR-0028)
```
사용자   → 지금 구현하고 설계를 이어가는 과정과, 설계를 끝낸 뒤 구현하는 과정이 각각 어떻게 평가될까
AI       → 처음엔 질문을 구현 순서 제안으로 잘못 읽음 → 다시 비교. 내 관점·요구사항 문장 관점 모두 (가)
검토 AI  → 결론은 (가). 요구사항 자체가 타임아웃을 지연 Mock 으로 정하라고 해 "설계 완료 후 구현"과 맞지 않음.
           AI 가 든 약점 둘은 근거가 없거나 원인을 잘못 짚음
대조     → 코드 없이도 ADR Verification 과 파일이 네 곳 어긋남(0005·0020·0021·0022). 미룬 Verification 은 12 개
결과     → 구현 단위로 갈림길 규칙 적용. 핵심 흐름을 막는 Q1·Q2·Q5·Q6 부터 설계. Verification 짝 규칙과 구현 단위 AI 기록을 CLAUDE.md 에
```

### 구현 단위: JVM 툴체인 25 (ADR-0022)
| | |
|---|---|
| AI 가 쓴 것 | `app/build.gradle.kts` 의 `jvmToolchain(21)` → `jvmToolchain(25)` 한 줄. 사용자 위임으로 Homebrew `openjdk@25`(25.0.4.1) 설치, `~/Library/Java/JavaVirtualMachines/openjdk-25.jdk` 에 연결 |
| 읽으며 물은 것 | `jvmToolchain` 이 런타임까지 정하는가 → 컴파일·테스트·`bootRun` 은 툴체인 JDK 로, `java -jar` 직접 실행은 그 명령의 JDK 로 돈다고 공식 문서로 확인 |
| 바꾼 것 | 없음 |
| 설명하지 못해 뺀 것 | 없음 |
| 확인 | `./gradlew clean build` 성공, 클래스 major version 69, `bootRun` 로그 "using Java 25.0.4.1" |

### Verification 점검 방식 (ADR-0029)
- 첫 구현 단위를 "기반 구성"으로 묶었다가, 쓰는 코드가 없는 스타터·설정을 미리 넣는 것이라 ADR-0006 과 부딪침을 발견
- ADR-0028 의 짝 규칙(Verification 이 가리키는 파일을 바로 맞춤)을 따르면 ADR-0006 을 어기게 됨
- 사용자 판단: 서둘러 맞추는 규칙이 이상하다. 모아서 한 번에 점검. 점검 시점은 정하지 않음
- 순서 조정: WebClient 스타터는 목록 동기화에서, 웹 스타터·가상 스레드 설정은 검색 API 에서 넣음. 이번 단위는 Mock 모듈

### 구현 단위: Mock 공급사 모듈 (ADR-0005)
| | |
|---|---|
| 정한 것 | 응답 값은 스펙 구조만 따르고 직접 만듦(부록 예시 값을 공개 저장소에 그대로 두지 않음). 부록이 짚은 상황은 똑같이 담음: 두 공급사가 같은 숙소를 다른 코드·이름·조식 조건·총액으로 팖, 한쪽에만 있는 숙소의 하루 재고 0. 모드는 정상·장애·무응답 + 지연(초). Mock 에도 가상 스레드. 목록 API 장애 모드는 넣지 않음(ADR-0019) |
| AI 가 쓴 것 | `settings.gradle.kts` 에 모듈 추가, `mock-supplier` 빌드 파일, 애플리케이션·컨트롤러·고정 응답, `application.yml`(포트 9090, 가상 스레드) |
| 읽으며 물은 것 | 우리 데이터로 만들어도 요구사항과 충돌하지 않나 → 부록은 예시를 그대로 쓸 필요 없고 구조만 지키면 된다고 함. Boot 4 웹 스타터 이름은 Boot 4.1.1 소스와 마이그레이션 가이드로 `webmvc` 확인(`web` 은 deprecated) |
| 바꾼 것 | 없음 |
| 설명하지 못해 뺀 것 | 없음 |
| 확인 | curl 로 네 엔드포인트 200·JSON, A 장애 503, B 장애 200+E503, 지연 2초, 무응답 3초 초과, 잘못된 모드 400. 무응답 중 스레드 덤프에서 요청 스레드 `virtual: True` |
| 알아 둘 것 | 요청 파라미터를 보지 않고 2026-10-05~10-08(3박) 고정 재고를 줌. 앱 검색을 확인할 때 이 날짜로 요청해야 함 |

### 공급사 응답 DTO (ADR-0030)
```
합의 1   → 스펙의 모든 필드를 받음 (사용자가 앞서 정한 방향)
합의 2   → 모르는 필드는 무시. AI 는 Jackson 3 기본값이라 설정이 필요 없다고 했으나, 사용자가 기본 동작도 명시하자고 함
           → DTO 마다 애너테이션 + 이유 주석. 전역 설정은 우리가 받는 요청까지 관대해져 뺌
합의 3   → AI 는 응답 틀은 DTO 에서 엄격하게 두자고 함. 사용자: 외부 응답을 DTO 에서 자르면 장애를 일부 허용할 수 없고, 판단은 DTO 의 역할이 아님
           → DTO 는 받기만, 틀까지 판정은 내부 로직에서 ADR-0027 기준. 문법·타입 오류만 역직렬화 실패
합의 4   → 필드 이름은 응답 예시대로. 설명 표의 대문자 표기는 실수로 봄
```

## Day 4 (2026-09-16)

### 공급사 어댑터 구조 (ADR-0031)
```
Q5-1     → 기능별 경계 둘 + 공급사당 한 클래스. 사용자가 판단이 어렵다며 독립 검토 요청
검토 AI  → 같은 결론. 다만 좁은 경계의 이유는 버그 방지가 아니라 소비자·테스트의 의존 모양이고,
           ADR-0014(검색이 목록 갱신을 부르지 않음)를 타입으로 강제하는 것이 구체적 이점.
           한 기능을 빠뜨리면 ADR-0019 때문에 공급사가 조용히 빠지므로 묶는 인터페이스는 필수
대조     → Spring 주입·순서, Kotlin override 규칙, WebClient 반환 타입 모두 원문 확인
Q5-2     → 어댑터는 공급사마다 다른 것까지, 공통 판정 규칙(ADR-0023·0027)은 공통 로직
Q5-3     → 목록 조회도 Mono. 기다리는 자리는 동기화 서비스
Q5-4     → 경계 타입 목록으로 주입. 목록 순서에 기대지 않음
구현 순서 → 목록 동기화 단위에서는 목록 경계만. 재고 경계와 묶는 인터페이스는 Q1·Q2 가 정해지는 검색 단위에서.
           껍데기를 미리 두는 안은 미정인 내용을 전제하게 되어 쓰지 않고, 붙을 자리만 주석으로 남김
```

### 매핑 저장 방식 (ADR-0032)
- DB 에 하는 일이 매핑 읽기·쓰기뿐이라 ORM 의 지연 로딩·변경 감지·연관 탐색을 쓸 자리가 없음
- 자주 쓸 "여러 건을 한 번에, 있으면 두고 없으면 넣기"는 SQL 이 짧고 의도가 드러남 → `JdbcClient`
- Spring Data JDBC 도 후보였으나 집합체 저장 규칙을 따로 이해해야 하고, 우리가 만든 식별자의 새 행 판단 문제는 JPA 와 비슷하게 남음
- 익숙함은 JPA 가 앞선다는 점을 잃는 것으로 기록

### 스키마 변경 도구 (ADR-0033)
- Flyway 12.4.0 / Liquibase 5.0.3 / 도구 없이 `schema.sql` 셋을 비교
- 사용자가 "Liquibase 는 더 복잡하고 더 자유로운 쪽인가"라고 물어 되돌리기 축을 확인함.
  Flyway 의 undo 는 유료 판, Liquibase 의 rollback 명령은 무료 판에도 있음(단 SQL 형식 변경 로그에는 직접 쓴 되돌리기 정의 불가)
- DB 를 PostgreSQL 로 고정했으므로 중립 형식의 이점이 작고, SQL 을 그대로 쓰는 쪽이 JdbcClient 선택과 같은 결이라 Flyway

### 내부 식별자 컬럼 타입 (ADR-0034)
- 사용자 반론("DB 는 느슨하게, 검증은 코드에서")을 다른 AI 에게 검증 요청
- 그 관행이 잘 맞는 자리는 CHECK 제약·DB 열거 타입·외래 키였고, 컬럼 타입은 값의 종류를 정하는 것이라 성격이 다름
- 문자열 컬럼은 같은 UUID 를 대소문자에 따라 다른 행으로 받아들임. `uuid` 는 표준형으로 정규화
- 형식을 바꿀 때 드는 비용(테이블·인덱스 재작성, ACCESS EXCLUSIVE 잠금)은 공식 문서로 확인. 우리 규모에서는 초 단위라는 측정도 받음(재현은 안 함)
- 파라미터를 문자열로 넘기면 타입 불일치로 실패한다는 점을 잃는 것에 기록

### 로컬·테스트 DB 실행 (ADR-0035)
- 로컬은 compose 파일을 앱이 함께 띄우고, 테스트는 Testcontainers 로 PostgreSQL 컨테이너
- H2 인메모리 안은 뺌. H2 문서가 다른 DB 흉내는 "차이의 일부만 구현"이라고 적고 있어, SQL 과 Flyway 스크립트를 직접 쓰는 이 설계에서는 통과해도 실제와 다를 수 있음
- 사용자가 Testcontainers 사용 경험을 물어 확인: 컨테이너는 테스트마다 새로 뜨는 게 기본이 아니라 선언 방식이 정하고, 스프링 컨텍스트 캐시로 여러 테스트가 공유함
- 그래서 테스트 격리는 새 컨테이너가 아니라 데이터 정리로 얻는다는 점을 넘길 것으로 기록

### 매핑 테이블 구성 (ADR-0036)
```
질문 1   → 객실 타입 내부 식별자 형태. AI 가 UUID 로 전제해 그려 지적받음 → 선택지 다시 내고 무작위 UUID 로 합의
질문 2   → 객실 타입 행은 내부 숙소 식별자로 숙소를 가리킴(공급사 코드는 숙소 테이블에만)
질문 3   → 사용자: 무작위 UUID 는 쓰기 비용이 크다고 들었다. 숫자 PK + 반환용 UUID(루티 방식)는?
확인     → 루티는 MySQL 에서 PK=IDENTITY + identifier VARCHAR(255)(무작위 UUID 문자열), 나중에 인덱스 추가.
           InnoDB 는 PK 가 행의 저장 순서이고 보조 인덱스마다 PK 를 담아 긴 무작위 키의 대가가 큼.
           PostgreSQL 은 행을 인덱스 순서로 저장하지 않아 대가가 인덱스 쪽에 한정. 공식 문서에 느리다는 서술은 없음(측정도 안 함)
결과     → 내부 식별자를 그대로 PK 로(A). 부담이 드러나면 UUIDv7 → 숫자 PK 순으로 손보기로 기록
```

### 목록에서 사라진 숙소 (ADR-0037)
```
AI 안    → 지우면 "항상 같은 식별자" 요구가 깨진다 → 사용자 반박으로 과한 읽기 인정, 근거 교체
검토 AI  → (가) 남기고 표시. 고객 응답은 세 안이 같고 차이는 되돌릴 수 있느냐뿐. 신원과 존재를 나눠 보라는 틀 제안
대조     → Booking.com(재고 비면 자동 닫힘·채우면 자동 열림), ETG(닫혀도 시스템에 남김), ONDA(disabled 와 deleted 구분) 원문 확인.
           Expedia·Agoda 인용은 확인 못 해 제외. 빈도 수치는 어디에도 없음
사용자   → 같은 식별자를 어떻게 다시 붙이나. 이름으로 판단하나 → 아니라고 정리(코드가 안정적이므로 코드로 찾음)
사용자   → 그럼 즉시 삭제를 원해도 못 하는데 맞는 기획인가 → 고객 노출은 즉시 멈춤. 기록 삭제 수단 없음을 잃는 것에 명시
결과     → 행은 남기고 `missing_since` 표시, 검색에서 제외. 숙소·객실 타입 둘 다. 표시는 규약대로 읽은 동기화에서만. 삭제 수단은 두지 않음
```

### 목록 동기화 구현 방식 (ADR-0038)
- 기동 시 동기화는 앱 기동을 막지 않음. AI 가 기동을 막는 안도 선택지로 놓자 사용자가 "내부 데이터 정리 중이라고 앱을 멈추냐"고 지적
- 공급사 주소·키는 설정 파일의 공급사별 묶음. 저장소에는 Mock 값만 두고 실제 값은 환경 변수로 덮어씀
- 트랜잭션은 공급사 하나의 동기화 전체. 실패 단위(ADR-0019)와 같고 사라진 행 표시의 원자성(ADR-0037)도 지켜짐
- 타임아웃 값은 Mock 지연 모드로 측정한 뒤 정하기로 넘김

### 구현을 누가 맡을지
- 선택지: 내가 구현 / Fable 이 구현 / 둘 다 구현 후 합치기 / 내가 구현하고 Fable 이 검토
- 사용자가 "Fable 이 코딩을 더 잘한다고 들었다"고 해 공식 모델 문서를 확인함.
  Opus 5 는 "For complex agentic coding and enterprise work", Fable 5.1 은 "For demanding reasoning and long-horizon agentic work" 로 적혀 있고,
  기본은 Opus 로 시작하고 까다로운 추론이나 긴 호흡 작업에 Fable 을 쓰라고 안내함
- 결과: 결정의 맥락을 들고 있는 Opus 가 구현하고, 구조 단계와 코드 단계에서 Fable 이 검토. CLAUDE.md 일하는 순서에 검토 단계 추가

### 목록 동기화 구현 전 남은 판단 (ADR-0039)
```
Fable 구조 검토 → ADR 위반 3건: 타임아웃 부재(ADR-0019 충돌), 공통 형태 non-null(ADR-0031 분담 충돌),
                  의존성 누락(flyway-database-postgresql·jackson-module-kotlin·HikariCP 7.1.0). 인용은 모두 원문 대조
판단 1   → 타임아웃 값은 공급사별 설정 묶음에. OpenFeign·Resilience4j 도 대상별 설정 + 공통 기본값 형태
판단 2   → 값을 정할 수 없어 뺀 항목도 missing_since 가 찍힘. 결과가 같아 구분하지 않고 ADR-0037 에 한 줄
판단 3   → Mock 목록 API 에도 모드 적용(구현·커밋 완료). 없으면 ADR-0037·0038 Verification 과 타임아웃 측정을 못 함
판단 4   → 뺀 것은 결과값으로 돌려주고 공급사별 로그 한 줄까지만. 원본 보관은 Q17·Q18 에서
판단 5   → 테스트는 테이블을 비워 되돌림. 트랜잭션 롤백은 동기화의 커밋·롤백 자체를 가려서 제외
표현 정정 → 사용자 지적으로 "규약 위반" → "스펙과 다른 응답", "값을 정할 수 없는 항목"으로 바꿈.
           "격리"는 요구사항의 선택 항목 이름이라 그 항목을 가리킬 때만 쓰고, 평소에는 "버리지 않고 따로 보관"으로 씀
```

### 구현 단위: 목록 동기화
| | |
|---|---|
| AI 가 쓴 것 | 매핑 스키마(Flyway V1)와 compose 파일, 의존성(JDBC·드라이버·Flyway·PostgreSQL 모듈·WebClient·Kotlin 용 Jackson·Testcontainers), 공급사 A·B 어댑터와 DTO, 공통 형태·인터페이스, 정규화, 저장소, 동기화 서비스와 스케줄러, 설정 클래스, 단위 테스트 6건과 통합 테스트 4건 |
| 읽으며 물은 것 | Boot 4.1.1 이 관리하는 Testcontainers 는 2.0.5 이고 모듈 이름이 `testcontainers-postgresql` 로 바뀐 것, `PostgreSQLContainer` 가 `org.testcontainers.postgresql` 로 옮겨진 것, Flyway 는 PostgreSQL 전용 모듈이 따로 필요한 것, HikariCP 판올림이 실제로 반영됐는지(7.0.2 → 7.1.0) |
| 바꾼 것 | 사용자 지적으로 최대 수용 인원이 1 미만이면 그 객실 타입을 빼도록 규칙을 더함. bootRun 의 실행 디렉터리를 루트로 맞춰 compose 파일을 찾게 함 |
| 막힌 곳 | 타임아웃 값. Mock 정상 응답이 1.2~1.6ms 라 측정으로 정할 수 없었다. 중간 장비의 기본 읽기 한계(nginx·ALB 60초 안팎)보다 짧게 두기로 하고 30초로 정함(ADR-0039) |
| 확인 | `./gradlew :app:bootRun` 이 compose 로 PostgreSQL 을 띄우고 Flyway 가 테이블 두 개를 만듦(psql 로 컬럼·제약·외래 키 확인). Mock 을 띄운 상태에서 A 는 숙소 2건·객실 타입 2건, B 는 1건·1건이 저장됨. A 를 장애 모드로 두면 A 만 건너뛰고 A 의 매핑은 그대로 남음. `./gradlew :app:test` 에서 단위 6건·통합 5건 통과 |

### 동기화 로그의 숫자 (ADR-0039 보완)
```
관찰     → 장애 모드 실행에서 목록이 그대로인 공급사 B 에 "사라짐표시=1" 이 찍힘
원인     → 반영은 그 공급사 행을 먼저 모두 표시하고 이번 목록에 있는 것을 되살리는 순서인데,
           표시 단계의 건수를 그대로 로그에 냈음. 되살아날 행까지 센 숫자였다
결과     → 반영이 끝난 뒤 표시가 남은 행을 세고 로그 이름을 "목록에없는숙소"로 바꿈. 통합 테스트 1건 추가
```

## Day 5 (2026-09-17)

### 구현 단위: 패키지 구조 정리 (ADR-0040)

| | |
|---|---|
| AI 가 쓴 것 | ADR-0040, `mapping` 패키지(저장소와 그 입력 형태), 옮긴 파일들의 package 선언과 import |
| 읽으며 물은 것 | Kotlin 의 `internal` 이 패키지가 아니라 모듈 단위라 패키지 사이 의존 규칙을 컴파일러가 막지 못한다는 것, `@ConfigurationPropertiesScan` 이 애노테이션을 선언한 클래스의 패키지부터 스캔한다는 것(그래서 설정 클래스를 옮겨도 안전) |
| 바꾼 것 | 저장소가 목록 전체(`NormalizedCatalog`) 대신 공급사와 숙소 목록을 받도록 서명을 바꿈. 뺀 항목은 동기화 쪽 개념이라 저장소까지 가지 않게 함 |
| 막힌 곳 | 없음. 다만 의존 방향을 도구로 강제하지 못해 `grep` 으로 확인하는 상태다 |
| 확인 | `./gradlew :app:test` 단위 6건·통합 5건 통과. `supplier` 와 `mapping` 안에서 다른 패키지를 import 하는 곳이 없음. Mock 을 띄우고 `bootRun` 했을 때 두 공급사가 모두 동기화되고(`목록 동기화 완료 supplier=a 숙소=2 객실타입=2 목록에없는숙소=0 제외=0`, b 는 1·1) `hotel_mapping` 에 3행이 `missing_since` 없이 저장됨 |

### 결정은 됐는데 코드에 없던 것들 (ADR-0029 점검)

Verification 절을 훑어 이미 결정된 동작인데 확인할 곳이 없던 자리를 채웠다. 새로 정한 것은 없다.

```
어긋남 1 → ADR-0016 은 "주기 기본값은 하루 한 번"인데 코드에 기본값이 없고 설정 파일 값에만 의존했음.
           @Scheduled 자리표시자에 기본값을 넣음. 값을 추측할 수 없어 설정을 필수로 둔 타임아웃(ADR-0039)과는 다른 경우
테스트 1  → 응답을 읽지 못한 동기화가 기존 표시를 건드리지 않는지 (ADR-0037)
테스트 2  → 공급사를 하나 더 붙여도 소비자 코드를 고치지 않는지 (ADR-0031)
테스트 3  → 스펙에 없는 필드는 무시하고, 읽을 수 없는 본문은 오류가 되는지 (ADR-0030)
테스트 4  → 저장 도중 실패하면 그 공급사 매핑이 이전 상태로 남는지 (ADR-0038).
           실패 주입은 DB 가 받지 못하는 값으로 했다. 저장소를 테스트용으로 바꾸지 않고 실제 트랜잭션 경계를 지나가게 하려고
```

**드러난 미정** — 문법은 맞지만 숙소 목록 필드가 통째로 없는 응답(`{}`)을 어떻게 볼지가 정해지지 않았다.
지금은 빈 목록으로 읽어 그 공급사 숙소가 모두 사라진 것으로 표시된다. 테스트로 고정하지 않고 ADR-0030 Verification 에 확인하지 못한 것으로 적었다.

### 구현 단위: 2차 독립 검토와 처리

목록 동기화와 패키지 정리를 묶어 다른 모델의 에이전트에게 다시 맡겼다. 받은 지적을 원문과 대조해 처리했다.

```
받아들임 1 → 어댑터가 실패를 한 신호로 모으지 않고 있었음. 감싸는 확장 함수를 두고 두 어댑터에 붙임
받아들임 2 → 테스트 소스의 널 문자. 이스케이프로 교체
받아들임 3 → 롤백 테스트에 예외 종류 단언이 없고, 실패가 첫 행에서 나 되돌려진 것이 표시뿐이었음
받아들임 4 → 객실 타입 사라짐 표시를 보는 테스트가 없었음(1차 지적, 이번에 확인됨)
받아들임 5 → 주기 기본값이 코드와 설정 파일 두 곳에 같은 값으로 있었음. 설정 파일에서 지우고 주석만 남김
받아들임 6 → ADR-0030 의 미정 서술이 공급사 A 기준이었음. B 는 이미 실패로 봄
받아들임 7 → ADR-0038 확인 항목을 코드 위치로 바꿔 놓았음. 무응답 모드로 실제 관찰하기로 되돌림
미룸       → 여러 건 upsert(배치)는 규모 근거가 없어 그대로 둠. 다시 볼 조건을 ADR-0038 에 남길지 검토 중
```

| | |
|---|---|
| AI 가 쓴 것 | 실패를 한 신호로 감싸는 확장 함수, 테스트 4건(객실 타입 표시 2건, 뺀 항목 표시, 공급사 실패가 아닌 예외), 어댑터 테스트의 타입 단언 3건 |
| 읽으며 물은 것 | `Mono` 의 타입 파라미터가 `Any` 를 상한으로 두고 있어 확장 함수에도 같은 경계가 필요하다는 것(컴파일이 막아 알게 됨), `ON CONFLICT DO UPDATE` 로 갱신된 행도 `RETURNING` 이 돌려준다는 것 |
| 바꾼 것 | 저장소가 숙소 식별자를 추가 조회 대신 upsert 응답에서 받게 함. 사라짐 표시 두 문장의 조건을 같게 맞춤 |
| 막힌 곳 | 없음. 편집 도구가 `\u0000` 표기를 자꾸 실제 널 문자로 바꾸어, 그 줄만 다른 방법으로 고쳤다 |
| 확인 | `./gradlew :app:cleanTest :app:test` 단위 6건·통합 12건·어댑터 6건 통과. Mock 의 공급사 A 를 무응답 모드로 두고 `bootRun` 하니 기동이 2.7초에 끝나고 30초 뒤 그 공급사만 타임아웃으로 실패했으며 B 는 같은 주기에 반영됨 |

### 구현 단위: 3차 독립 검토와 처리

2차 지적을 처리한 상태를 다시 맡겼다. 처리 결과가 맞는지와 새로 생긴 문제를 봐 달라고 했다.

```
받아들임 1 → 감싸는 자리가 응답 변환 뒤라 우리 쪽 오류까지 덮고 있었음. 변환 앞으로 옮김
받아들임 2 → 설정 키를 지우면서 그 키를 가리키는 ADR-0016 Verification 을 안 고쳤음
받아들임 3 → 저장 실패가 동기화 서비스를 지나는 테스트가 없었음. 통합 테스트 1건 추가
받아들임 4 → 기동 관찰 기록에 재현 절차가 없고 수치를 판정 기준처럼 적었음
받아들임 5 → 배치를 미룬 것은 타당하나 다시 볼 조건이 없었음. 공급사 한 곳의 숙소 수로 적음
올림       → 타임아웃과 연결 실패를 "공급사 실패"로 볼지는 코드가 이미 한쪽을 전제하는데 ADR 에 없다.
             내가 정하지 않고 판단 대기로 올렸다
```

| | |
|---|---|
| AI 가 쓴 것 | 감싸기 위치 수정과 원인 클래스 이름, 저장 실패 통합 테스트 1건, ADR-0038 의 재현 절차·판정 기준·배치 조건, ADR-0016 Verification |
| 읽으며 물은 것 | `onErrorMap` 이 상류 오류만 본다는 것. 그래서 변환 앞에 두면 전송·역직렬화·타임아웃만 감싸고 변환 코드의 오류는 그대로 지나간다 |
| 바꾼 것 | 실패 로그의 두 가지가 실제로 갈리게 됨. 앞 커밋에서 나눠 놓고도 감싸기가 그것을 되돌리고 있었다 |
| 막힌 곳 | 편집 도구가 `\u0000` 표기를 실제 널 문자로 바꾸는 일이 반복된다. 이 파일을 고친 뒤에는 널 바이트를 세어 보고 있다 |
| 확인 | `./gradlew :app:cleanTest :app:test` 단위 6건·통합 13건·어댑터 6건 통과 |

### 구현 단위: 4차 독립 검토와 처리

```
받아들임 1 → JOURNAL 에 실제 널 바이트 2개가 커밋돼 있었음. 널 문자 실수를 적은 그 줄이다
받아들임 2 → 커밋 확인 줄에 적은 SupplierFailureTest 를 ADR-0027 Verification 에 옮기지 않았음
받아들임 3 → 감싸기 자리를 옮긴 뒤 KDoc 의 "이미"가 틀린 말이 됐고, 조건 하나는 지나갈 경로가 없어졌음
받아들임 4 → 기동 관찰의 판정 기준이 로그 순서만이라, 기동 로그 뒤에서 막는 구현을 가려내지 못했음.
             실패 로그의 스레드(scheduling-1)를 기준에 더함. 재현 절차에 Mock 먼저 띄우기와 되돌리기도 빠져 있었음
받아들임 5 → 예외를 만드는 곳이 둘이 아니라 셋(동기화 서비스의 빈 응답 처리 포함)
확인만    → 저장 실패 테스트는 정규화를 통과해 저장에서 실패하고 단언도 충분하다는 것,
             ADR-0016 의 환경 변수 이름이 자리표시자 경로에서 동작하는 형태라는 것
올림      → asSupplierFailure 의 조건을 지울지 방어로 남길지. 내가 정하지 않고 판단 대기로 올렸다
```

검토자의 의견은 **목록 동기화 단위를 이쯤에서 마무리해도 된다**는 것이었다. 남은 것으로 셋을 들었고 그중 둘을 이번에 처리했다.

### 구현 단위: 값 검증을 객체 생성자로 (ADR-0041)

```
정함 1 → 목록을 담는 필드가 없는 응답은 그 공급사 실패로 본다. 빈 목록과 구분한다
정함 2 → 객실 타입이 하나도 없는 숙소는 제외하고 사유를 남긴다
정함 3 → 값 검증은 그 값을 담는 객체의 생성자에 둔다. 정적 팩토리는 그 생성자를 부른다
깨뜨림 → 없는 인원을 0 으로 바꿔 넘겨 사유가 "1 미만"으로 기록됐고, 중복 검사를 앞으로 옮겨
         코드 없는 항목들이 한 그룹으로 묶였다. 둘 다 전에 되던 것이 깨진 것이고 검토가 잡았다
```

| | |
|---|---|
| AI 가 쓴 것 | ADR-0041, 두 값 객체의 불변식과 정적 팩토리, 정규화 재작성, 테스트 12건 |
| 읽으며 물은 것 | `data class` 의 생성자를 비공개로 하면 `copy()` 가 그것을 노출하고 언어 버전 2.5 에서 오류가 된다는 것(문서에 없어 직접 컴파일해 확인) |
| 바꾼 것 | 사용자 지적으로 불변식을 생성자에 둠. 어댑터의 `orEmpty()` 를 걷어내 없는 목록과 빈 목록을 구분해 넘김 |
| 막힌 곳 | 편집 도구가 이스케이프 표기를 실제 널 문자로 바꾸는 일이 반복됨. 커밋 전 텍스트 파일 전체를 세는 것으로 절차를 바꿈 |
| 확인 | `./gradlew :app:cleanTest :app:test` 정규화 11건·통합 14건·생성자 6건·어댑터 9건·감싸기 4건 통과. Mock 을 띄운 실행에서 A 숙소 2·객실타입 2, B 1·1, 제외 0 |

### 문서의 사실 주장만 따로 검사 (Day 5)

코드 검토를 네 번 받아도 남는 종류가 있어 인용 전수 대조를 따로 맡겼다. 85건을 원문과 열어 대조했다.

```
틀림 5      → Flyway 인용문(절반만 따옴), Kotlin "테스트하는"→"완전히 지원하는",
               JDK 22~24 Premier 종료(한 시점이 아니라 셋), MySQL 유사도, Boot 3.4 최신 패치
위치 틀림 2 → main 브랜치 줄 번호 둘. 확인하지 않은 새 번호를 적는 대신 파일·이름으로 바꿈
근거 없음 7 → 배포 점유율, 옛 좌표의 증상, 동시 요청이 몰린다는 전제, 관행의 실재 등.
               확인된 범위로 좁히거나 우리 판단임을 밝힘. 둘은 사실이어서 출처만 보탬
약함 4      → 릴리스 노트로는 알 수 없는 것을 확인했다고 적은 것 등
```

이 검사를 CLAUDE.md 검토 기준 12번으로 넣었다. 범위를 좁힐 때 관점 목록까지 줄이지 않는다는 문장도 함께 넣었다.

### 구현 단위: Verification 모아서 점검 (ADR-0029)

서술만 있고 확인할 곳이 없던 ADR 열세 곳을 테스트 이름·파일 경로·실행 관찰로 채웠다. 결정은 새로 하지 않았다.

```
채운 것 → 0002·0005·0007·0011·0013·0017·0018·0022·0032·0033·0034·0036·0040
찾은 것 → 옛 문장 둘이 사실과 달랐다. ADR-0002 는 JPA 플러그인을 확인하라고 적어 두었는데
           매핑 저장을 JPA 로 하지 않기로 했고(ADR-0032), ADR-0007 은 `jvmToolchain(21)` 을 가리키는데 25 로 올렸다
남은 것 → 검색·재고 단위(0010·0012·0014·0015·0019·0020·0021·0023~0026)와 절차 ADR. 그 단위에서 채운다
```

확인처를 쓰면서 **확인하지 않은 것을 적을 뻔한 자리가 둘 있었다.** `grep` 결과를 "supplier 안쪽뿐"이라고 적었는데
실제로는 `mapping` 은 한 줄도 없고 `supplier` 하위만 부모를 가리켰다. JDK 경로도 기억으로 적으려다 남은 실행 로그에서 확인했다.
사실 대조 검사에서 지적받은 습관이 바로 이것이라, 쓰기 전에 파일로 확인하는 것을 절차로 두었다.

### 검색 설계의 나머지 결정 (ADR-0046 ~ 0050)

검색을 막던 질문을 하나씩 닫았다. 다섯 결정 중 넷에서 AI 의 첫 추천이 뒤집혔고, 뒤집힌 이유가 같은 종류다.

```
Q25 상태·건수 (0046)   → 상태 둘 + 스펙 제외 건수. 매핑 제외는 세지 않는다
Q26 값 객체 자리 (0047) → 요청 형태와 숙박 구간·인원은 supplier, 표준 모델 값 객체는 search
Q27 인원 불변식 (0048)  → 각각 0 이상, 합 1 이상. 성인 최소 인원 없음
Q29 식별자 자리 (0049)  → 어댑터만 갖는다. Fetched·Normalized 에서 뺀다
Q31 묶음 일부 실패 (0050) → 성공한 묶음은 내보내고 실패한 묶음 수를 싣는다. 실패는 응답을 못 만들 때만
```

**AI 가 틀린 방식 셋.** 모두 "모르는 것을 아는 것처럼" 또는 "모양만 보고 이유를 안 따라간" 것이다.

| 어디서 | AI 가 한 말 | 사용자가 짚은 것 | 실제 |
|---|---|---|---|
| Q27 (1차) | "아동만의 예약은 성립하지 않는다" | "원래 불가능한가? 근거를 조사해봐" | 한국은 법 금지가 아니다. 글로벌 규격 관행일 뿐 ([조사](docs/research/guest-occupancy.md)) |
| Q27 (2차) | "스펙에 성인 0 동작이 없어 거절되면 공급사 실패로 집계된다" | "그 말이 사실이야?" → "없다는 사실이 아니지, 그냥 모르는 거지" | 스펙은 `int` 라고만 한다. 모르는 것은 어느 선택지의 근거도 아니다 |
| Q29 (1차) | "새 공급사가 식별자를 네 자리에 적는다" | "어댑터에 한 번만 넣으면 흐름대로 전달되는 거 아닌가?" | 맞다. 참조 줄 수를 적는 자리 수로 말했다 |
| Q29 (2차) | (나)를 권하며 "파일 다섯을 고치는 대가" | "수정이 대가인 건 말이 안 되고, 결정이 맞는지만 보면 돼" | 수정량은 근거가 아니다. 흐름을 그리자 (가)가 더 나은 코드였다 |
| Q31 | "(가)가 지금 결정들과 모양이 맞는다" | "나머지까지 버리는 게 맞나? 트랜잭션 단위가 어떻게 되더라" | 목록이 공급사 단위였던 이유(반만 남아 틀린 사실이 됨)가 검색에는 없다 |

Q29 에서 사용자가 "어디서 어딜 호출하고 어떻게 전달되는지 한 흐름으로 정리해 달라"고 했고, 그 그림을 그리자 답이 보였다.
**선택지를 놓기 전에 흐름부터 그리는 것**이 순서였다.

말에서도 셋 걸렸다. `missing_since` 를 "사라짐"이라고 불러 헷갈리게 했고(용어집에 컬럼 이름 그대로 부르라고 내가 적어 둔 것),
"형태를 씁니다"를 반복해 무엇을 만드는지 안 보였고, 판정 설명에서 "어댑터가 합산하면 틀린 총액이 나온다"는 가상의 코드를
실제 일어나는 일처럼 써서 "기록하고 뺐는데 왜 계산해?"라는 질문을 받았다.

### 구현 단위: 검색 (값 객체 → 경계 → 판정 → 서비스 → 엔드포인트)

```
커밋 22개, 테스트 44 → 105
값 객체 5   → StayPeriod · GuestCount (supplier) / Money · Rate · RateConditions (search)
경계       → AvailabilityAdapter · SupplierAdapter(묶음) · AvailabilityRequest · FetchedAvailability
어댑터     → 두 공급사에 fetchAvailability. 요금은 더하지 않고 준 모양(Daily/Total) 그대로
판정       → AvailabilityNormalizer. ADR-0027 두 표의 줄 하나가 테스트 하나
서비스     → 공급사 동시, 묶음 50개 × concurrency, 공급사별 시간 한계, 부분 실패 합치기
엔드포인트  → GET /api/v1/stays/search. 값 객체가 거부하면 400 에 그 문장
Mock       → 코드 51개 이상은 공급사 모양대로 거절
```

| | |
|---|---|
| AI 가 쓴 것 | 위 전부와 테스트 61건, ADR 5개, 조사 2개, ADR-0027 요금 표 |
| 읽으며 물은 것 | Boot 4 는 `@WebMvcTest` 가 `spring-boot-webmvc-test` 로 갈라졌다 (starter-test 가 안 끌고 옴). `mockito-kotlin` 이 없어 정확한 인자로 stub 했다 |
| 바꾼 것 | 실행해 보니 실패 사유가 "모든 묶음 호출이 실패했다"만 나가 503 인지 타임아웃인지 응답에서 사라졌다. 묶음 실패의 원인을 사유에 싣게 고침 |
| 설명 못 해 뺀 것 | 없음 |
| 확인 | `./gradlew :app:test` 105건. Mock 으로 정상·B 장애·A 무응답(5.01초에 끊김)·잘못된 입력 네 경우를 실제 호출 |

**측정.** 타임아웃 값을 정하려고 쟀는데 값이 아니라 관계식이 나왔다 ([기록](docs/research/search-latency.md)).
`검색 시간 ≈ ceil(묶음 수 ÷ 동시 실행 수) × 호출 지연`. 숙소 3,000개·호출 1초면 15초라 8초 한계를 넘는다.
세 값은 숙소 수를 모르고는 정할 수 없어 임시값으로 두고 그 사실을 ADR-0045 에 적었다.

측정 도중 실수 하나. `seq -s,` 가 끝에 쉼표를 붙여 50개를 보냈다고 생각한 요청이 51개였다. Mock 이 50개를 거절한 줄 알고
Mock 을 의심했는데 입력이 틀린 것이었다. 결과가 이상하면 **입력부터** 보는 게 맞았다.

### 검색 단위 독립 검토와 처리

지적 일곱 중 다섯이 사실이었고 둘은 "확인 못 함"으로 표시된 것이었다. 무거운 둘.

```
결함 → 숙소 코드가 없는 항목이 "매핑에 없다"로 분류됐다. 매핑 누락을 응답 건수에서 빼는
       근거가 "다음 동기화에서 끝난다"인데 코드가 없는 것은 끝나지 않는다. 스펙 위반이 건수에서 샜다
어긋남 → ADR-0021 이 "블로킹 DB 호출을 이벤트 루프 밖으로 옮기는 스케줄러를 따로 두지 않는다"고
       적어 놓고 코드가 Schedulers.boundedElastic() 을 쓰고 있었다. 코드를 결정 쪽으로 맞췄다
문서   → 용어집이 "HTTP 응답은 검색 결과를 그대로 내보낸다"고 했는데 코드는 반대였다.
       뜻을 정하는 곳이 틀린 말을 하고 있었다
```

스케줄러를 걷어내면서 잃는 것 하나가 생겼다. 매핑 읽기가 늦어지면 검색 시간 한계가 그 구간은 끊지 못한다.
끊으려면 스케줄러를 두어야 하는데 그건 ADR-0021 이 하지 않기로 한 것이라, 숨기지 않고 그 ADR 에 적었다.

### 선택 항목: 재시도 (ADR-0051)

값을 먼저 정하려다 사용자가 순서를 바꿨다. "값은 나중에 튜닝하고 구현 방식이랑 설계만 먼저하자."
그렇게 하니 설계가 오히려 깨끗해졌다. 값에 기대던 이유가 드러나 더 나은 이유로 바뀌었기 때문이다.

```
처음 이유 → 타임아웃은 재시도 안 한다. 5초+5초면 검색 시간 한계 8초를 넘으니까
바뀐 이유 → 타임아웃은 "공급사가 느리다"는 신호다. 느린 것을 다시 불러도 느리다
            값이 바뀌어도 이 판단은 안 바뀐다
```

사용자 질문 둘이 설계를 바꿨다.

| 질문 | 드러난 것 |
|---|---|
| "그 실패 신호는 어디서 나누어서 담는 거야? 어댑터?" | 어댑터 한 곳이 아니라 둘이었다(본문 코드는 어댑터, HTTP·네트워크는 공통 자리). 그리고 이름이 틀렸다 — `retryable` 이 아니라 `transient` 다. **재시도할지는 실패의 성질이 아니라 부르는 쪽의 사정**이다 |
| "예산이라는 게 뭐야? 사용자의 인내심이야?" | AI 가 budget 을 "예산"으로 옮겨 쓰고 있었다(문서에는 "검색 전체 시간 한계"로 적혀 있다). 그리고 **8초에 근거가 전혀 없다**는 것이 드러났다. [Nielsen 의 10초](https://www.nngroup.com/articles/response-times-3-important-limits/)를 나중에 찾았는데, 8초가 그 아래인 건 우연이었다 |

| | |
|---|---|
| AI 가 쓴 것 | ADR-0051, `transient` 판정 두 자리, 검색의 재시도, 테스트 11건 |
| 읽으며 물은 것 | `resilience4j-spring-boot4` 는 Maven Central 에 없다(최신 릴리스가 2.3.0). 검색 요약은 "2.4.0 에 있다"고 했는데 사실이 아니었다. Reactor 의 `Retry.backoff` 는 무작위를 기본(0.5)으로 넣지만 최대 대기는 기본이 사실상 무한이라 반드시 걸어야 한다 |
| 막힌 곳 | 테스트가 재시도를 못 봤다. 가짜 어댑터가 `Mono` 를 한 번만 만들어서다. 재시도는 같은 `Mono` 를 **다시 구독**하는 것이라 밖에서 한 번 세면 안 보인다. `defer` 로 감쌌다 |
| 확인 | `./gradlew :app:test` 116건. Mock 으로 503 은 재시도 2회(132ms 간격), 무응답은 재시도 0회 |

값에 근거를 못 붙인 것을 그대로 적었다. 시도 3회만 [Google SRE](https://sre.google/sre-book/handling-overload/) 를 빌렸고 대기 시간 둘은 임시값이다.

### 다음에 할 것
**정해야 할 것**
- 서킷 브레이커를 할지, 한다면 라이브러리를 쓸지. 사실은 모아 뒀다 (`resilience4j-circuitbreaker` 단독은 core+slf4j 로 가볍고, `-reactor` 모듈은 reactor 3.4 기준이라 우리 3.8 과 맞는지 미확인)
- 나머지 선택 항목: 캐시·격리·병합·통화·예약 대행
- Q28 체크인일이 지난 날짜인지 검사할지, 한다면 어느 시간대 기준으로. 지금은 검사 안 함
- Q17·Q18·Q19 — 검색을 막지 않음

**할 것** — 값 튜닝(선택 항목을 마친 뒤). README (사용자가 마지막에 검토). Feature Spec 의 `status: draft` 와 `last_reviewed` 갱신

