# 진행 기록

이 파일에 의사결정과 AI 활용을 함께 적는다. 처음엔 AI 활용만 따로 `docs/ai-log.md`에
적었는데, 이 프로젝트는 혼자 개발이고 거의 모든 코드를 AI와 함께 써서 "무슨 일이
있었나"와 "AI와 무슨 일이 있었나"가 사실상 같은 이야기였다. 같은 사건을 두 파일에
다른 각도로 두 번 적게 되어 합쳤다.

## Day 0 (2026-09-13)

### 있었던 일

작업 방식을 정하기 전에 AI 가 요금 모델, 실패 분류, 타임아웃 값, 패키지 구조 같은
결정을 스스로 내리고 파일 15개를 만들어버렸다. 갈림길에서 멈추지 않고 진행한 것이
문제였다.

발견 즉시 멈추고 저장소를 커밋 0개 상태로 되돌렸다. 이미 쓴 코드는 사본만 남기고
버렸다. 되돌리는 데 몇 시간 분량의 코드를 잃었지만, 그 코드에 담긴 결정이 누구
것인지 불분명한 상태로 계속 쌓는 것보다 여기서 멈추는 게 낫다고 판단했다.

### 결정

이후로는 갈림길마다 AI 가 선택지와 각각 잃는 것, 추천과 그 추천의 가장 약한 점을
내고 사람이 결정한다. 결정은 ADR 로 남긴다(ADR-0001). AI 자율 실행은 없다.

## Day 1 (2026-09-14)

### 수행 내용

- ADR 형식 확정. 결정 하나에 파일 하나, 선택지에 AI 추천 표시, 결정과 다르면
  그 경위를 Discussion 에 남기는 형식으로 정리했다(ADR-0001)
- 언어를 Kotlin 으로 정했다(ADR-0002). AI 는 처음 Java 를 권했다. 근거는
  "AI 가 만든 Kotlin 관용구를 인터뷰에서 방어하기 어렵다"였는데, 설명 가능성을
  구현의 기준으로 두면 지금 이해도가 코드 품질의 상한이 되어 버린다는 지적을
  받고 추천을 바꿨다. 이해 못 하는 건 나중에 공부하면 된다는 원칙과 구현 기준은
  별개다
- CLAUDE.md 를 저장소에 공개하기로 하고(ADR-0003), 커밋 규약(ADR-0004)을 정했다
- Mock 공급사를 별도 Gradle 모듈로 두기로 했다(ADR-0005). 처음엔 `src/test` 에
  두려 했으나(모듈을 안 늘리고 컴포넌트 스캔도 회피할 수 있다는 이유), 재검토에서
  함정이 드러났다. `src/test` 클래스패스로 띄우면 앱의 main 리소스·의존성을
  그대로 물려받아, H2 파일 모드에서 앱과 Mock 이 같은 DB 파일을 열려다 락 충돌로
  죽는다. 별도 모듈로 바꿨다
- 무응답 Mock 을 `Thread.sleep` 으로 재현하는 방법은 그대로 받아들였다. 묶이는
  게 Mock 의 스레드이지 우리가 재는 앱 스레드가 아니기 때문이다
- 의존성은 그 기능을 만들 때 추가하기로 정했다(ADR-0006)
- 프로젝트 골격을 만들고 실제로 기동되는 것까지 확인했다
- 빌드 도구·프레임워크 버전을 정했다(ADR-0007). 이 과정에서 AI 가 낸 근거 여러
  개가 검증 과정에서 뒤집혔다(아래 막힌 지점 참고). 자세한 경위는 ADR-0007 의
  Discussion 에 있다
- 사람 1명 + AI 협업에 맞는 개발 방법론을 조사시켰다. Spec Kit·Kiro 같은
  spec-driven development 도구는 거부했다. 목적이 사람을 검토 루프에서 빼는
  것이라 이 프로젝트가 가려는 방향과 반대라는 조사 결과를 따랐다. ADR 형식과
  "구현 직후 코드를 읽고 대안을 따져보기"는 채택했다

### 막힌 지점

- 저장소 이름과 패키지 이름을 정할 때, AI 가 GitHub 핸들을 옛날 값(`9rrong`,
  숫자로 시작해 패키지 세그먼트로 쓸 수 없는 값)으로 잘못 전제하고 판단했다.
  실제 핸들은 `goohong`이라 확인 후 다시 판단받았다. 이름 선택이 AI 자신의
  코드로 이미 구현된 쪽에 편향될 수 있다는 우려가 있어서, 이 대화와 결정 이력을
  모르는 별도 세션에 같은 질문을 다시 물어 대조했다. 실제로 패키지 추천이
  한 번 바뀌었다(자신 없다고 스스로 밝힌 그대로 편향이 있었던 것)
- Gradle 버전을 9.4.1 로 정했다가, "9.4.1 이 9.7.1 보다 나은 점이 있느냐"는
  질문에 답을 못 찾아 9.7.1 로 바꿨다. "이미 검증했다"는 관성이지 근거가
  아니었다. Kotlin 버전도 같은 방식으로 재검증했더니 "최신이라서"가 아니라
  "우리가 고른 Gradle 을 지원 범위에 담는 유일한 KGP 라인이라서"가 진짜
  근거였다
- Boot 3→4 로 올릴 때 AI 가 "webflux 스타터와 `web-application-type: servlet`
  설정이 필요하다"고 했는데 틀렸다. Boot 3 도 webmvc+webflux 가 같이 있으면
  자동으로 SERVLET 으로 판정한다. 진짜 단점은 그 설정이 필요해서가 아니라
  안 쓰는 리액티브 서버 의존성이 딸려오는 것이었다
- `jvmToolchain(21)` 이 없는 JDK 를 자동으로 받아오는지를 두고 AI 가 두 번
  틀렸다. 처음엔 "없으면 빌드 실패"라고 단정했다가, 스스로 "Gradle 이 자동으로
  받아온다"고 뒤집었는데 이것도 틀렸다. 직접 재확인한 결과 그 JDK 는 이번에
  받아진 게 아니라 몇 달 전 다른 프로젝트 작업으로 이미 캐시에 있던 것이었고,
  `foojay-resolver-convention` 플러그인 없이는 자동 다운로드가 안 된다는 걸
  공식 문서로 확인했다
- 코드를 짚혔을 때 설명하지 못하면 어떻게 할지를 두고, AI 가 "더 단순한
  코드로 다시 쓴다"를 규칙으로 제안했는데 거부했다. 그러면 지금 이해도가
  구현 가능한 코드의 상한이 되어 버린다. 이해 못 하는 코드는 나중에 공부하면
  되는 것이지 구현 자체를 못 하게 막을 이유는 아니다
- AI 활용 기록과 이 기록을 만드는 걸 하루 종일 미뤘다. ADR 을 쓰면서 그 옆에
  같이 적었어야 했는데, 다음 작업으로 넘어가려던 시점에야 만들었다. 그러면서
  `docs/ai-log.md` 를 따로 만들었다가, 이 파일과 역할이 겹친다는 지적을 받고
  다시 하나로 합쳤다

### 다음에 할 것

표준 모델(금액, 숙박 구간, 재고, 판매 조건) 설계. Feature Spec 을 쓸지 여부를
그 단계에서 정한다.
