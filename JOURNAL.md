# 진행 기록

의사결정과 AI 활용을 함께 적는다. 처음엔 AI 활용만 `docs/ai-log.md`에 따로
적었는데, 이 프로젝트는 혼자 개발이고 거의 모든 코드를 AI 와 함께 써서
"무슨 일이 있었나"와 "AI 와 무슨 일이 있었나"가 같은 이야기였다. 합쳤다.

각 결정은 이 표기로 남긴다.

```
AI 안 → 반박 → 검증 → 결과
```

## 주고받은 반박 모음

사용자와 AI 가 서로의 주장을 반박하거나 정정한 곳을 시간순으로 모았다. 대화 기록 원본에서 찾아 정리했다.
자세한 경위가 ADR 이나 아래 날짜별 기록에 있으면 마지막 칸에 적었다.

- **짚은 쪽**: 사용자 = 사용자가 AI 주장을 반박 / AI = AI 가 사용자 생각을 반박 / 검토 AI = 맥락을 따로 받은 다른 AI 세션 / 스스로 = AI 가 검증하다 자기 주장을 뒤집음

### 작업 방식 (9/13)

| 짚은 쪽 | 처음 주장 | 반박 | 결과 | 자세히 |
|---|---|---|---|---|
| 사용자 | AI 가 직무 설명 원문 없이 계획을 세움 | 직무 설명이 중요한데 없이 어떻게 계획했나 | 과거 기록에서 원문을 찾아 반영 | — |
| 사용자 | AI 가 승인 없이 코드를 쓰기 시작함 | 같이 승인하면서 가기로 한 것 아닌가 | 저장소를 커밋 0개로 되돌림 | Day 0 |
| 사용자 | AI 가 "여러 개를 묶어 한 번에 승인"하는 방식과 자율 단계를 제안 | 자율로 하길 원하지 않는다. 하나하나 같이 따지자 | 갈림길마다 선택지 제시 → 사용자 결정 | ADR-0001 |
| 사용자 | AI 가 가용 시간을 정하고, 사용자가 직접 타이핑하는 절차를 제안 | 시간을 자원으로 두지 말라. 직접 타이핑의 목적이 대충 볼까 봐서라면 걱정 말라 | 시간 제약과 직접 타이핑 절차를 뺌 | — |
| 사용자 | AI 가 이미 쓴 코드를 유지하고 소급 검토하자고 권함 | 이미 내린 결정에 편향되지 않을 자신이 없으면 되돌리라 | 되돌리고 처음부터 같이 정함 | Day 0 |
| 사용자 | AI 가 "원장", "소유", "관통", "끝에서 끝" 같은 번역어를 씀 | 억지 번역이라 이해가 안 된다. 원래 영어 기술 용어를 쓰라 | 기술 용어는 영어 그대로 쓰기로 함 | — |
| 사용자 | AI 가 "설명 못 하면 더 단순한 코드로 다시 쓴다"를 규칙으로 제안 | 설명 가능성이 구현 기준이 되면 안 된다. 모르면 공부하고, 모르면 승인하지 않겠다 | 규칙에서 뺌 | ADR-0002 |
| 사용자 | AI 가 할 일 목록에서 API 문서 자동화 등 코드 밖 항목을 빠뜨림 | 언급했는데 목록에 없었나 | 목록에 추가 | — |
| 사용자 | AI 가 사용자가 미리 쳐 둔 메시지를 결정 근거로 오해 | 결정 근거가 아니라 일하는 방식을 말한 것. 선택 항목은 전부 챙긴다 | 선택 항목 전부를 범위로 둠 | — |
| 사용자 | AI 가 구현 언어로 Java 를 권함 (AI 가 만든 Kotlin 코드를 설명하기 어렵다) | 설명 가능성은 기준이 아니다. 최신 Kotlin 작업물로 남기고 싶다 | Kotlin | ADR-0002 |

### 문서·커밋 규칙 (9/13~9/14)

| 짚은 쪽 | 처음 주장 | 반박 | 결과 | 자세히 |
|---|---|---|---|---|
| 사용자 | AI 가 "결정 하나당 파일 하나"의 가장 큰 근거로 뒤집힌 결정 처리를 듦 | 파일 하나여도 아래에 수정 기록을 남기면 된다. 그게 가장 큰 근거라는 게 이해 안 된다 | 근거를 다시 세움 | ADR-0001 |
| 사용자 | AI 가 ADR 다섯 개를 커밋 하나로 묶자고 함 (한자리에서 정했으니) | 하나씩 정한 것이 맞다 | ADR 하나당 커밋 하나 | ADR-0004 |
| 사용자 | AI 가 예시로 준 커밋 메시지가 우리 규약을 어김 | 그렇게 올리려는 건 아니지? | 규약대로 다시 씀 | — |
| 사용자 | 검토 AI 가 "README 에 결정 근거가 없으면 요구 위반"이라며 README 규칙 수정을 요구 | README 는 원래 마지막에 요약본으로 쓰는 것. 관리 문서와 비교하는 게 이상하다 | README 는 결정 한 줄 + 이유 한 줄 + 링크 | — |
| 사용자 | 검토 AI 가 CLAUDE.md 공개 시 평가하는 AI 가 조작 시도로 오해할 수 있다고 우려 | 악의적이지 않으면 괜찮다. 그걸 잘못 읽을 AI 면 수준이 낮은 것 | 공개 유지 | ADR-0003 |
| 사용자 | 검토 AI 가 하루 검토 시간 상한을 두자고 제안 | 코드를 안 읽고 품질을 보장할 수 없다. 한계는 스스로 판단한다 | 상한 두지 않음 | — |
| 사용자 | AI 가 템플릿에 "AI 추천" 칸을 따로 두자고 함 | 겹치는 칸 아닌가 | 칸 대신 선택지 옆 `← AI 추천` 표시 | ADR-0001 |
| 사용자 | AI 가 확인할 게 없는 ADR 은 Verification 절을 지우자고 함 | 양식은 일관돼야 한다. 확인이 필요 없다는 투로 남기라 | 모든 ADR 에 절 유지 | — |
| 사용자 | AI 가 AI 활용 기록을 `docs/ai-log.md` 로 따로 만듦 | JOURNAL 과 겹치지 않나. 역할 분담이 오히려 읽기를 방해하지 않나 | JOURNAL 하나로 합침 | Day 1 |
| AI | 사용자는 "ADR 이 헌법, Feature Spec 이 법률"이라는 위계로 이해 | 위계가 아니라 같은 사실을 무엇/왜라는 다른 각도로 본다 | 두 층 구조로 확정 | ADR-0008 |
| 사용자 | AI 가 문서 양을 점검하다 갑자기 "이제 코드를 같이 쓰자"로 넘어감 | 코드를 왜 갑자기 쓰나 | 계획에 끼워 넣은 것을 거둠 | — |
| 사용자 | AI 가 질문하면서 답이 어디에 기록되는지 말하지 않음 | 내 답을 어디에 넣으려는지도 알려 달라 | 질문마다 기록 위치를 밝힘 | — |

### 저장소·스택 (9/14)

| 짚은 쪽 | 처음 주장 | 반박 | 결과 | 자세히 |
|---|---|---|---|---|
| 스스로 | AI 가 GitHub 핸들을 옛 값으로 전제하고 패키지 이름을 추천 | 실제 핸들 확인 결과 전제가 틀림 | 다시 판단 | Day 1 |
| 검토 AI | AI 의 패키지 이름 추천 | 맥락 없이 같은 질문을 받은 다른 AI 가 다른 답을 냄 | `com.stayaggregator` | Day 1 |
| AI | 사용자는 멀티 모듈이 너무 크다고 봄 (예전 프로젝트에서 빌드 설정이 꼬인 경험) | Mock 을 테스트 코드 안에 두면 앱 설정·DB 파일을 같이 물려받아 충돌 | Mock 은 별도 모듈 | ADR-0005 |
| AI | 사용자는 테스트용 모킹 라이브러리면 충분하다고 봄 | 요구사항은 연동 동작을 실제로 띄워 확인하므로 별도 포트의 Mock 서버가 필요 | 독립 Mock 서버 | ADR-0005 |
| 사용자 | AI 가 날짜 처리 문제와 네 번째 장애 시나리오를 모듈 결정에 섞어 말함 | 모듈과 무관한데 그냥 얘기한 건가 | 별개 문제로 분리 | — |
| 사용자 | AI 가 Gradle·Spring Boot 버전을 혼자 정함 | 같이 논의했어야 한다. 근거와 함께 남기자 | 버전 결정 ADR 작성 | ADR-0007 |
| 사용자 | AI 의 근거가 "안정적인 것 중 최신", "JDK 21 이 로컬에 있다" | 그게 근거인가. 로컬에 있다는 말은 말이 안 된다. 제대로 조사했나 | 공식 문서·JEP·호환표로 다시 조사 | ADR-0007 |
| 스스로 | AI 가 "JDK 가 없으면 빌드 실패" → 다시 "Gradle 이 자동으로 받는다" | 둘 다 틀림. 캐시에 있던 JDK 였고, 자동 다운로드는 플러그인이 있어야 함 | 근거 교체 | ADR-0007 |
| 사용자 | AI 가 Spring Boot 4 의 약점으로 "자료가 적다"를 듦 | 나는 실사례보다 공식 문서로 판단한다 | 약점의 무게가 줄어 Boot 4 채택 | ADR-0007 |
| 스스로 | 검토 AI 가 Gradle 9.7.1 이 실행 환경 위험이 더 작다고 함 | 공식 호환표상 9.4.1 과 실행 JVM 범위가 같음 | 그 근거는 기각 | ADR-0007 |
| 사용자 | AI 가 "이미 검증했으니 9.4.1 유지" | 9.4.1 이 9.7.1 보다 나은 근거도 없지 않나 | 9.7.1 | ADR-0007 |
| AI | 사용자는 네 버전이 모두 LTS 라고 봄 | LTS 는 JDK 에만 있는 개념. 나머지는 라인별 지원 기간 | ADR 에 LTS 로 잘못 쓰지 않음 | ADR-0007 |
| 사용자 | AI 의 Kotlin 버전 근거가 "최신이고 문제없다" | 그 검증을 해 달라는 것 | 우리가 고른 Gradle 을 지원 범위에 담는 버전이라는 근거로 교체 | ADR-0007 |

### 요구사항·기록 (9/14)

| 짚은 쪽 | 처음 주장 | 반박 | 결과 | 자세히 |
|---|---|---|---|---|
| 사용자 | AI 가 요구사항 문서의 필요한 절만 골라 읽고 설계를 논의 | 가장 중요한 문서다. 최소 세 번 읽고 대화하라 | 전체 3회독 후 오픈 질문 13개로 확장 | Day 2 밤 |
| 사용자 | AI 가 조사 도중 사이트 접속이 차단됐다며 멈춤 | 그럼 브라우저로 직접 보라 | 브라우저로 화면을 직접 확인 | Day 2 밤 |
| 사용자 | AI 가 커밋 시각을 실제 결정 시각으로 옮기는 안을 선택지로 꺼냄 | 원래 적힌 시각을 그대로 유지하라는 뜻이었다 | 원래 시각 유지 | Day 2 밤 |
| 사용자 | AI 가 요구사항 문서와 몇 글자 겹친다고 자연스러운 문장을 고침 | 이 작업의 성격이 드러나지만 않으면 괜찮다. 목적에서 벗어난 행위를 위한 행위 | 겹침 검사를 커밋 조건에서 뺌 | Day 2 밤 |
| 사용자 | AI 가 방향 의견을 요청받자 남은 시간을 근거로 "정하자마자 구현", "선택 항목은 설계만" 제안 | 합의한 방식을 왜 마음대로 깨나 | 거둬들임 | Day 2 밤 |

### 도메인 설계 (9/14)

| 짚은 쪽 | 처음 주장 | 반박 | 결과 | 자세히 |
|---|---|---|---|---|
| 사용자 | AI 가 A·B 두 공급사만 보고 요금 필드를 짬 | 공급사가 늘어날 걸 전제하기로 한 원칙은 어디 반영됐나 | 공급사별 변환을 어댑터로 분리 | Day 2 |
| AI | 사용자는 예전 프로젝트의 컴포짓 패턴을 떠올림 | 그건 여럿 중 하나를 골라 쓰는 폴백이고, 여긴 전부 불러 합치는 구조 | 어댑터 + 병렬 호출 | Day 2 |
| 사용자 | AI 가 "공급사 2~3개면 코루틴과 Reactor 성능 차이가 없다" | 공급사가 많아질 환경을 전제해야 하지 않나 | 동시 호출 수 제한이 근거가 되어 Reactor | ADR-0009 |
| AI | 사용자가 Semaphore 를 직접 쓰는 이점을 물음 | 공급사별 세밀한 제어는 얻지만 지금 확인된 요구가 아니다 | Reactor 연산자로 제한 | ADR-0009 |
| 사용자 | AI 가 금액 값 객체를 "오버엔지니어링 위험"으로 표시 | 값 객체는 오히려 좋은 패턴이다. 잃는 게 클래스 하나뿐 아닌가 | 값 객체 채택, 오버엔지니어링 표현 철회 | — |
| AI | 사용자가 음수 금액을 막자고 함 | 할인·환불은 음수로 표현하고 싶어질 수 있다 | 금액은 0 이상, 음수 개념은 별도 개념이나 메서드로 | — |
| 사용자 | AI 가 통화 코드 검증 위치를 정하지 않고 넘어감 | 금액 객체가 문자열을 받아 직접 검증하는 게 낫지 않나 | 금액 객체 안에서 검증 | — |
| 사용자 | AI 가 "세금 포함 총액이 필수"를 정해진 것처럼 말함 | 나는 답한 적 없다. 요구사항에 맞는가, 응답값이 총액인가 | 요구사항은 우리 판단에 맡겼음을 확인하고 다시 따짐 | — |
| 사용자 | AI 가 1박 평균은 "나누기라서 지어낸 값"이라고만 설명 | 평일·주말 가격이 다를 수 있다. 전제부터 따져 보자 | 예시 데이터에서도 날짜별 요금이 다름을 확인. 조회 단위도 기간 전체 | — |
| 스스로 | AI 가 총액 하나를 기준 필드로 올리자고 함 | 조건이 다른 요금을 싸다고 비교하게 만들 수 있다 | 요금과 조건을 한 덩어리로 | — |
| 사용자 | AI 가 알 수 없는 조건 표시 필드까지 제안 | 조식까지만 비교 대상으로 두고 나머지는 어댑터가 통제 | 조건은 조식만 표준으로 | — |
| 사용자 | AI 가 공급사 원본을 표준 모델에 넣자고 함 | 어댑터를 거친 뒤 원본을 쓸 일이 없다 | 표준 모델에서 원본 제외 | — |
| 사용자 | AI 가 원본을 로그로 남기자고 함 | 요구사항 중 전체 원본 로그가 필요한 곳이 있나. 조식만 특별 대우할 가치가 있나 | 요구사항·업계 조사 후 판단 | research/rate-display |
| 사용자 | AI 가 "보존"을 원본 저장으로 해석해 여러 차례 논의 | 공급사 DTO 가 모든 필드를 받는다는 뜻이었다 | 오픈 질문 Q6 | Day 2 밤 |
| 사용자 | 요금 모델 논의가 요금을 저장하는 것처럼 들림 | 요금·재고는 저장 안 하는데 식별자 말고 저장할 게 있나 | 저장은 매핑뿐, 요금은 검색마다 메모리에서만 | Day 2 밤 |
| AI | 사용자는 캐시를 필수로 봄 | 요구사항 분류상 선택 항목이다 | 논의 전 | — |
| 사용자 | AI 가 "공급사 코드와 한 테이블이면 병합할 자리가 없다" | 행 두 개를 들고 있으면 되지 않나 | AI 주장이 틀렸음을 인정 | ADR-0010 |
| AI | 사용자가 자연 키 기본 키의 장점이 쓰기 속도인지 물음 | 쓰기 속도는 자연 키의 장점이 아니다. 오히려 불리한 경우가 많다 | 설명으로 정리 | — |
| 사용자 | AI 가 결정 질문에서 테이블·키·인덱스 설계로 내려감 | ERD 가 아니라 결정을 먼저 하려던 것 | 결정 수준으로 질문을 다시 세움 | ADR-0010 |
| AI | 사용자는 병합 시 식별자를 하나로 통일하는 방식을 떠올림 | 식별자를 받는 쪽이 그걸로 예약한다 | 사용자가 "식별자가 같으면 안 되겠다"고 판단해 식별자 유지 | ADR-0010 |
| 사용자 | AI 가 내부 식별자를 공급사 코드로 계산하는 방식을 권하며 트레이드오프를 "코드가 바뀌면 식별자도 바뀐다" 하나로 설명 | 트레이드오프가 그뿐인가 | 공급사 코드 역추적, 입력 표기 고정 등 큰 것 두 개를 더 찾음 | ADR-0011 |
| 사용자 | AI 가 계산 방식의 이점으로 "데이터를 잃어도 같은 값"을 듦 | 비밀 키 계산은 성능 부담이 없나. DB 를 엎는 상황까지 유지해야 하나, 마이그레이션이 있지 않나. 같은 방을 여러 개 넣을 수는 없나 | 측정해 보니 성능 부담 없음. 마이그레이션은 저장값을 옮기므로 이점이 드문 사고 대비로 좁아짐 | ADR-0011 |
| 사용자 | AI 가 "이미 매핑된 숙소에 새 객실 타입이 목록 갱신 전 조회에 먼저 나올 수 있다"는 시나리오를 흐름 설명 없이 제시 | 그게 가능한가. 우리가 저장하는 것과 조회에 넣는 값, 받아 쓰는 값이 뭔가 | 조회는 숙소 코드로만 하고 객실 타입은 응답으로 받는 흐름을 요구사항 기준으로 다시 설명 | Day 3 |
| 사용자 | AI 가 설명 중 아직 정하지 않은 저장 구조(키 표기, 컬럼 구성, 중복 방지)를 정해진 것처럼 말함 | 안 정한 걸 정한 것처럼 머릿속 구조를 꺼내지 말라. 헷갈려서 계속 지체된다 | 설명마다 요구사항·결정됨·미정을 구분 | Day 3 |
| 사용자 | AI 가 정적 정보를 목록 기준으로 하되 최대 수용 인원만 조회 값을 쓰는 예외를 권함 | 매핑이 핵심인데 예외를 두는 느낌이다. 최대 인원은 드물게 바뀐다. 바뀐 정보는 공급사가 요청하게 나중에 구현하면 된다 | 예외 없이 목록 기준. 모순은 알려진 한계, 공급사 알림은 범위 밖 확장으로 기록 | ADR-0012 |

## Day 0 (2026-09-13)

### 사건: 승인 없이 진행한 것을 되돌림

- **무슨 일**: 작업 방식을 정하기 전에 AI 가 요금 모델·실패 분류·타임아웃
  값·패키지 구조를 스스로 정하고 파일 15개를 만듦
- **문제**: 갈림길에서 멈추지 않고 진행함
- **조치**: 발견 즉시 저장소를 커밋 0개로 되돌림. 코드는 사본만 남기고 버림
- **왜 되돌렸나**: 몇 시간 분량을 잃더라도, 결정이 누구 것인지 불분명한
  채로 계속 쌓는 것보다 낫다고 판단

### 결정

| 결정 | 내용 |
|---|---|
| 작업 방식 | 갈림길마다 AI 가 선택지 + 잃는 것 + 추천 + 약점을 내고 사람이 결정 |
| 자율 실행 | 없음 |
| 기록 | ADR 로 남김 (ADR-0001) |

## Day 1 (2026-09-14)

### 정한 것

| # | 결정 | ADR |
|---|---|---|
| 1 | ADR 형식 확정 (결정 하나-파일 하나, AI 추천 표시, Discussion 절) | 0001 |
| 2 | 언어 = Kotlin | 0002 |
| 3 | CLAUDE.md 저장소 공개 | 0003 |
| 4 | 커밋 규약 | 0004 |
| 5 | Mock 공급사 = 별도 Gradle 모듈 | 0005 |
| 6 | 의존성은 기능 만들 때 추가 | 0006 |
| 7 | 빌드 도구·프레임워크 버전 | 0007 |

### 뒤집힌 것들

**언어(Kotlin)**
```
AI 안    → Java 권함. "AI 가 만든 Kotlin 관용구를 인터뷰에서 방어하기 어렵다"
반박     → 설명 가능성을 구현 기준으로 두면 지금 이해도가 코드 품질의 상한이 됨.
           모자란 건 공부하면 됨
검증     → 근거를 빼면 남는 건 2년 공백뿐. 10개월·1만 줄 경험에 비추어 결정적이지 않음
결과     → AI 가 Kotlin 으로 추천 변경
```

**Mock 배치 (`src/test` → 별도 모듈)**
```
AI 안    → src/test 에 두면 모듈 안 늘리고 컴포넌트 스캔도 회피
재검토   → src/test 클래스패스로 띄우면 앱의 main 리소스·의존성을 그대로 물려받음.
           H2 파일 모드에서 앱과 Mock 이 같은 DB 파일을 열려다 락 충돌
결과     → 별도 Gradle 모듈로 변경 (ADR-0005)
```

**Gradle 버전 (9.4.1 → 9.7.1)**
```
AI 안    → "9.4.1 이미 검증했으니 유지"
반박     → "9.4.1 이 9.7.1 보다 나은 점이 뭐냐" → 답 못 함. 관성이지 근거가 아니었음
검증     → 실행 JDK 지원 범위 동일(17–26). 9.7.1 에 버그 수정 포함, 실측 확인
결과     → 9.7.1 로 변경
```

**Kotlin 버전 근거 (2.4.20)**
```
AI 안    → "최신이고 Boot BOM 과 안 어긋남"
반박     → Gradle 에서 이미 기각된 "최신이라서" 논리와 같음
검증     → Gradle 9.7.1 을 지원 범위에 담는 KGP 버전은 2.4.20 이 유일
결과     → 근거만 교체 (결정은 동일)
```

**Boot 3→4 전환 근거**
```
AI 안    → "webflux 스타터 + web-application-type: servlet 설정 필요"
검증     → 틀림. Boot 3 도 webmvc+webflux 공존 시 자동으로 SERVLET 판정
정정     → 진짜 단점은 안 쓰는 리액티브 서버 의존성이 딸려오는 것
```

**JDK 자동 다운로드**
```
AI 안 1  → "JDK 21 없으면 빌드 실패"
AI 안 2  → (스스로 뒤집음) "Gradle 이 자동으로 받아온다"
재확인   → 둘 다 틀림. 그 JDK 는 몇 달 전 다른 프로젝트로 이미 캐시에 있던 것.
           foojay-resolver-convention 플러그인 없이는 자동 다운로드 안 됨
```

**저장소·패키지 이름**
```
AI 안    → dev.goohong 권장. 근거: GitHub 핸들이 9rrong(숫자 시작이라 패키지 불가)
검증     → 실제 핸들은 goohong (gh api 로 확인). 전제가 틀림
편향 확인 → 이 대화를 모르는 별도 세션에 같은 질문 재확인 → 패키지 추천이 실제로 바뀜
결과     → com.stayaggregator (기존 프로젝트들과 같은 방식)
```

### 그 외 거부한 것

| AI 제안 | 거부 이유 |
|---|---|
| "설명 못 하면 더 단순한 코드로 다시 쓴다"를 규칙으로 | 그러면 지금 이해도가 구현 가능한 코드의 상한이 됨. 이해 못 하는 건 나중에 공부하면 됨 |

### 반성

- AI 활용 기록을 하루 종일 미룸. `docs/ai-log.md` 를 따로 만들었다가
  JOURNAL 과 역할이 겹친다는 지적을 받고 다시 합침

### 다음에 할 것

표준 모델(금액, 숙박 구간, 재고, 판매 조건) 설계.

## Day 2 (2026-09-14, 계속)

### 문서 구조 재정비

**측정**: ADR 8개, 문서 927줄 vs 코드 11줄

**진단**: ADR 8개 중 4개(0001·0003·0004·0006·0008)가 도메인 판단이 아니라
작업 방식 자체에 관한 메타 ADR. 앞으로는 도메인 판단(요금·실패분류·타임아웃)만
ADR 로 남기기로 함.

**Feature Spec 도입** (ADR-0008)
```
처음 이해    → "ADR 이 헌법, Feature Spec 이 법률" (위계 관계로 오해)
정정         → 위계가 아니라 관점 차이. Feature Spec = 무엇을(What),
               ADR = 왜(Why). Feature Spec 은 관련 ADR 을 모아 보는 인덱스
```

### 확장성 원칙 미반영 발견

- **문제**: 첫날 정한 "공급사가 늘어날 걸 전제로 확장에 열어두기"가 코드에
  전혀 반영 안 됨. 요금 필드를 정하려는 시점에 A, B 만 알고 스키마를 짜면
  공급사가 늘 때마다 표준 모델을 계속 고쳐야 하는 문제로 드러남
- **해법**: 어댑터 패턴. 표준 모델은 "지금 응답이 필요로 하는 것"만 최소로,
  공급사별 원본은 각 어댑터가 sealed interface 로 보존. 새 공급사 = 어댑터
  구현체 하나 추가, 표준 모델은 안 바뀜
- **헷갈릴 뻔한 것**: 컴포짓 패턴(루티의 `PlaceSearcherComposite`)과 비교함

  | | 루티 Composite | 우리 필요 |
  |---|---|---|
  | 동작 | 여럿 중 쓸 수 있는 하나만 골라 씀 | 여럿을 전부 병렬로 불러 합침 |
  | 패턴 | 폴백 체인 | 어댑터 + fan-out |

### Reactor vs 코루틴 (ADR-0009)

```
AI 안    → "이 규모(공급사 2~3개)에선 성능 차이 없다" (요구사항의 지정 취지만 근거)
반박     → 이 도메인은 공급사가 많아질 걸 전제해야 함. "숙소 수천 개" 경고를
           스스로 무시한 판단이었음
재검증   → 수천 개 규모에선 동시 호출 수 제한이 실제로 필요.
           Reactor = flatMap(mapper, concurrency) 한 줄 / 코루틴 = Semaphore 직접 구현
결과     → 결정(Reactor)은 유지, 근거만 "요구사항 취지" → "수천 개 규모의 실질 필요"로 교체
```

### 다음에 할 것

Q1(금액 표현)을 어댑터 구조 위에서 구체화. `NormalizedRate` 와 `RateAdapter`
인터페이스를 실제 코드로 작성.

## Day 2 밤 (2026-09-14)

### 공개 저장소 점검과 히스토리 다시 쓰기
- **발견**: 문서에 이 저장소에서 쓰지 않기로 한 표현과, 요구사항 문서의 문장을 따옴표로 거의 그대로 옮긴 곳이 있었다. 한 ADR 을 커밋할 때 그 파일만 검사를 빼먹은 것이 시작이었다
- **조치**: 현재 파일을 고친 뒤, 과거 커밋까지 다시 써서 강제 푸시했다. 커밋 시각은 원래 기록 그대로 두었다
- **재발 방지**: 푸시 전 훅으로 금지 표현이 든 커밋은 푸시를 막는다
- **과하게 적용한 것**: 이어서 요구사항 문서와 몇 글자만 겹쳐도 문장을 고치다가, "이 작업의 성격이 드러나지만 않으면 괜찮다, 목적에서 벗어난 행위를 위한 행위"라는 지적을 받고 멈췄다

### 요구사항 문서를 부분만 읽고 논의한 것
```
문제     → 요금 모델을 몇 시간 논의하는 동안 요구사항 문서 3장을 필요한 절만 골라 읽었다
지적     → "가장 중요한 문서인데 아직 안 읽었다는 건 말이 안 된다. 최소 3회독"
조치     → 원본 전체 읽기 → 요구사항 목록 뽑기 → 원문과 한 줄씩 대조
새로 본 것 → 정렬·페이징은 범위 밖 / 서버 방식(WebFlux·MVC)을 고른 이유를 남겨야 함 /
            숙소 코드 50개 초과는 오류 / 숙소명이 목록·조회 두 API 에 다 있음 /
            같은 객실인데 공급사마다 객실명이 다름
결과     → Feature Spec 오픈 질문을 5개에서 13개로 늘림
```

### 도메인 조사
- 규제(한국·미국), 플랫폼 화면 세 곳, 업계 연동 규격 세 가지를 조사해 `docs/research/rate-display.md` 에 남겼다
- 조사로 확인한 것: 총액이 기준값이다 / 1박 가격의 뜻은 플랫폼마다 다르다 / 조식과 무료 취소가 요금 조건으로 공통으로 쓰인다

### "보존"을 잘못 옮긴 것
```
사용자 말 → 외부 응답은 보존해야 한다
AI 해석  → 원본을 저장·기록한다 (로그? DB? 표준 모델에 원본 필드?)
실제 뜻  → 공급사 응답 DTO 는 스펙의 모든 필드를 받아야 한다. 저장까지는 아니다
결과     → 원본 저장 논의 몇 차례가 헛돌았다. Q6 으로 올림
```

### 요금·재고는 저장하지 않는다
- 헷갈린 지점: 요금 표준 모델을 논의하다 보니 요금을 저장하는 것처럼 들렸다
- 정리: DB 에는 공급사 코드와 내부 식별자 매핑만 둔다. 요금·재고는 검색할 때마다 받아서 메모리에서만 표준 모델로 바꾸고 응답한 뒤 버린다

### 병합해도 내부 식별자 유지 (ADR-0010)
```
AI 안    → 내부 숙소 테이블과 매핑 테이블을 나눠야 병합할 자리가 생긴다
반박     → 행 두 개면 되지 않나? → 기본 키·인덱스까지 내려가자 "ERD 가 아니라 결정을 먼저"
검증     → 한 테이블로도 병합 가능. "자리가 없다"는 틀린 가정. 테이블 구조는 결정 뒤의 설계
결정 질문 → 병합하면 이미 발급한 내부 식별자를 바꿀 것인가
결과     → 바꾸지 않고 묶음 정보를 덧붙인다. 근거는 사용자가 든 "예약 대상은 공급사 상품이다"
```

### AI 가 합의한 방식을 깬 것
- 방향 의견을 요청받자 AI 가 남은 시간을 근거로 "정하자마자 구현", "선택 항목은 설계만"을 제안했다
- 처음에 시간을 제약으로 두지 않기로 했고, 작업 순서와 범위도 합의했었다. 지적받고 거둬들였다

### 멈춘 곳
Q13 내부 식별자 형태를 여쭤본 상태에서 멈췄다. 답은 아직 없다.
- (가) 순서대로 번호 발급 / (나) 무작위 식별자(UUID) 발급 ← AI 추천 / (다) 공급사와 공급사 코드로 계산
- AI 추천의 약점: 로컬 DB 를 새로 만들 때마다 식별자가 바뀐다. 이 상황에서 같은 값을 보장하는 건 (다) 뿐이다

### 다음에 할 것
Q13 결정 → Q8 (숙소명·객실 타입명·최대 수용 인원을 매핑과 함께 저장할지) → Q10 → Q11 → Q12

### 참고 자료
- [요금 표시와 요금 조건 조사](docs/research/rate-display.md)
- [빌드 도구·프레임워크 버전 조사](docs/research/build-versions.md)

## Day 3 (2026-09-15)

### 내부 식별자 형태 (ADR-0011)
```
AI 안 1  → 무작위 UUID 발급 (순서·개수가 안 드러남)
AI 안 2  → 공급사 코드로 계산으로 변경 (데이터를 잃어도 같은 값이 나온다는 이유)
반박 1   → 트레이드오프가 그뿐인가 → 역추적 위험, 입력 표기 고정, 같은 상품 중복 불가를 추가로 찾음
반박 2   → 비밀 키 계산 성능은? → 로컬 측정: HMAC 약 136ns, 무작위 UUID 약 137ns. 부담 없음
반박 3   → DB 를 엎는 상황까지 유지해야 하나, 마이그레이션이 있지 않나
검증     → 마이그레이션·백업은 발급 방식과 무관하게 저장값을 옮김. 계산 방식의 이점은 데이터 유실 후
           재구성이라는 드문 사고 대비뿐이고, 로직을 못 바꾼다는 제약은 그 이점의 뒷면
결과     → 무작위 UUID 를 발급해 저장. 사용자는 영향이 크지 않다고 보고 기본 방식을 택함
```

### 응답의 정적 정보 출처 (ADR-0012)
```
AI 안    → 목록 기준 저장값 + 최대 수용 인원만 조회값(예외) + 어긋나면 갱신 신호
중간 혼선 → AI 가 흐름 설명 없이 "새 객실 타입이 조회에 먼저 나온다"는 시나리오를 제시해 사용자가 되물음.
           이어서 정하지 않은 저장 구조를 정해진 것처럼 말해 한 번 더 되물음
정리     → 조회는 숙소 코드로만, 객실 타입은 응답으로 받음(요구사항). 이 흐름 위에서 시나리오 11개를 다시 봄
반박     → 매핑이 핵심인데 필드 하나에 예외를 두는 느낌. 최대 인원은 드물게 바뀜.
           바뀐 정보는 공급사가 갱신을 요청하는 방식을 나중에 두면 됨
결과     → 예외 없이 목록 기준. 최대 인원 증가 시 잠깐의 모순과 새 객실 타입 누락은 알려진 한계로,
           공급사 알림 방식은 이번 스펙에 없어 범위 밖 확장으로 기록. 갱신 신호는 Q10 에서
```

### 다음에 할 것
Q10 (숙소 목록 API 를 언제 부를지. ADR-0012 에서 넘어온 질문 포함)

