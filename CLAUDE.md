# 작업 규칙

여러 외부 숙박 공급사의 서로 다른 API 를 하나의 표준 모델로 통합해,
날짜와 인원으로 검색한 결과를 돌려주는 연동 백엔드입니다.

이 파일은 이 저장소에서 일할 때의 규칙을 적어 둔 것입니다.
코드를 AI 와 함께 작성하므로 그 규칙도 함께 둡니다. 공개한 이유는 README 에 적었습니다.

## 무엇을 어디에 적는가

| | |
|---|---|
| `docs/features/` | 기능이 무엇을 하는가(What). 오픈 질문과 결정 로그(ADR 링크만) |
| `docs/adr/` | 결정과 그 근거(Why). 선택지, 버린 대안, 버린 이유 |
| `docs/research/` | 조사한 사실. 출처·조회일·관찰만 적고 판단은 ADR 에 적습니다 |
| `CLAUDE.md` | 작업 절차. 계속 참조하는 규칙 본문 |
| `README.md` | 빌드·실행 방법. 결정은 한 줄 요약과 ADR 링크만 |
| `JOURNAL.md` | 날짜별 진행, 막힌 지점, AI 에게 무엇을 묻고 그 답을 어떻게 처리했는지 |

같은 내용을 두 곳에 쓰지 않습니다. 한쪽이 바뀌면 다른 쪽이 틀린 말이 되기 때문입니다.
Feature Spec 과 ADR 은 위계가 아니라 같은 사실을 다른 각도(무엇 vs 왜)로 봅니다.
Feature Spec 의 결정 로그에는 ADR 링크만 두고 선택지나 이유를 다시 쓰지 않습니다.

## 일하는 순서

1. 갈림길이 나오면 선택지와 각각 잃는 것을 정리해 합의합니다
2. 합의된 것을 ADR 로 남깁니다. 기준은 [ADR-0001](docs/adr/0001-record-decisions-as-adr.md)
3. 그 결정이 어느 Feature Spec 의 오픈 질문에 해당하면, 그 항목을 결정 로그로
   옮기고 ADR 을 링크합니다. 방향만 정하고 세부가 남았으면 오픈 질문에
   "상태"를 적어 두어 결정된 것처럼 보이지 않게 합니다
4. 구현합니다
5. 동작을 확인합니다
6. 커밋합니다

**합의되지 않은 갈림길이 남아 있으면 구현을 시작하지 않습니다.**
되돌리기 가장 쉬운 쪽으로 임시 구현하는 것도 하지 않습니다. 그 임시가 그대로 남기 때문입니다.

이 규칙은 구현 단위로 적용합니다. 구현하려는 단위가 닿는 갈림길이 모두 합의됐으면 시작하고,
열린 질문의 답을 전제하는 코드는 쓰지 않습니다. 설계는 핵심 흐름을 막는 질문부터 다룹니다.
기준은 [ADR-0028](docs/adr/0028-implement-decided-units-first.md) 에 있습니다.

구현 단위마다 JOURNAL 에 AI 가 쓴 것, 읽으며 물은 것, 바꾼 것, 설명하지 못해 뺀 것을 남깁니다.

**무엇을 하는 코드인지 설명할 수 없으면 커밋하지 않습니다.**

## 문서를 쓸 때

동작에 대한 서술에는 **확인할 수 있는 곳**을 붙입니다. 파일 경로나 테스트 이름입니다.

```
공급사 B 의 E503 은 A 의 HTTP 503 과 같은 실패로 분류한다
  → SupplierBAdapterTest.e503IsUpstreamError
```

확인할 곳이 없는 동작 서술은 쓰지 않습니다.
## 커밋

형식은 [ADR-0004](docs/adr/0004-commit-convention.md) 에서 정했습니다.

```
type(scope): 제목

본문

확인: ...

ADR-NNNN

Co-Authored-By: ...
```

### 제목

- 한국어로 쓰고 `type` 과 `scope` 만 영어입니다
- **동사로 끝냅니다.** `추가`, `채택`, `정의`, `단일화` 같은 동사성 명사
- 50자 이내. 마침표는 찍지 않습니다
- `~을 위해`, `~하도록` 같은 목적절은 제목에서 빼고 본문에 씁니다

`type` — `feat` `fix` `docs` `refactor` `test` `chore` `revert`

`scope` — `domain` `supplier` `catalog` `search` `mock` `infra` `adr` `readme` `journal`
문서 커밋의 scope 는 그 문서가 다루는 영역으로 합니다.

### 본문

**이 커밋 밖에 기록되지 않은 판단이 있을 때만** 씁니다.
무엇을 했는지는 diff 가 말하므로 본문에는 왜 그랬는지를 씁니다.

ADR 이나 다른 문서에 있는 내용은 베끼지 않고 참조만 합니다.
커밋 메시지는 나중에 못 고치는데 문서는 계속 고치므로, 베끼면 커밋이 틀린 말을 하게 됩니다.

### `확인:`

빌드나 실행 결과가 바뀌는 커밋에만 씁니다. 문서만 고치는 커밋에는 쓰지 않습니다.

명령이나 테스트 이름이나 관찰한 사실 하나를 적습니다.

```
확인: ./gradlew test --tests SupplierBAdapterTest
확인: 공급사 B 를 장애 모드로 두고 호출했을 때 응답의 suppliers[B].status 가 FAILED
```

"동작 확인함" 같은 문장은 쓰지 않습니다.

### `ADR-NNNN` 참조

- **제목의 번호** — 이 커밋이 그 ADR 을 만든다는 뜻
- **트레일러의 번호** — 이 커밋이 그 결정을 구현하거나 그 영향을 받는다는 뜻

트레일러가 셋 이상이면 커밋을 쪼개라는 신호로 봅니다.

### `Co-Authored-By`

AI 가 diff 의 일부라도 작성했을 때만 붙입니다.
논의만 하고 직접 작성한 코드에는 붙이지 않습니다.

### 커밋 단위

- "그리고" 없이 한 문장으로 말할 수 있는 것 하나
- 그 커밋만 되돌려도 빌드가 깨지지 않을 것
- 계약이나 동작이 바뀌는 커밋은 그것을 서술하는 문서 변경을 함께 담습니다.
  `JOURNAL.md` 는 별도 커밋으로 합니다
- 구현 커밋의 `확인:` 에 적은 테스트 이름은 같은 커밋에서 해당 ADR 의 Verification 에 적습니다
- 의존성을 추가하는 커밋은 본문을 씁니다. 왜 그 라이브러리이고 대안은 무엇이었는지
- 되돌릴 때는 `revert(scope): <원 제목>` 에 본문을 씁니다. 되돌리는 이유와 원 커밋 해시
